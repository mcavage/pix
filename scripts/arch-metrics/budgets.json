{
  "schema": 1,
  "packages": {
    "pix/host": {
      "prod_loc": 4815,
      "exports": 0,
      "globals": 52,
      "edges": 13,
      "exits": 20,
      "_prod_loc_note": "Then U1 of the self-development UAT auth repair (docs/design/self-development-uat.md): `pix-host uat-mcp`, spawned by the sbx gateway, becomes a dumb stdio<->Unix-socket relay (relayBytes) with no Runner and no os/exec, pinned by TestUatMcpGatewayIsADumbRelay; the real UAT Runner construction moves to a new `pix-host uat-worker` mode (uat_worker.go, uat_worker_construct.go), which will be started by `pix run --dev` in U2 so it inherits the operator's authenticated host context that gateway ancestry cannot carry. uatWorkerLoop accepts one connection at a time, builds a fresh per-connection MCPServer sharing the one long-lived Runner, and survives a client EOF (only that connection's Serve returns) without exiting. One new os.Exit path: uat-worker's dispatch case in main.go mirrors uat-mcp's existing os.Exit(2) on error. prod_loc 4607 -> 4771, globals 50 -> 52 (uatConnectAttempts/uatConnectDelay), exits 18 -> 19. A deliberate, reviewed raise of a shrink-only budget. Then the daemon runtime: a third [[services]] runtime for a long-running HOST process that already speaks its own protocol on a loopback port. It exists because go-plugin is the wrong shape for one -- snow-proxy is reached over HTTP by an in-sandbox wrapper, so a net/rpc handshake would replace a working transport to gain nothing -- and because the Port/Listen/Health fields on Service were designed for exactly this and had no runtime that read them. It reuses everything that matters (staged copy-then-verify, env allowlist, restart budgets, the permanent-vs-transient split, and the rule that a running process is not health) and adds only launch + probe. The doctor row is the point: a pack daemon was the one host-exec facet with NO row, so when snow-proxy died the warehouse capability degraded in total silence. Then the third launch form. UnitSpec described only two ways to start a unit -- self-exec or a SHA-pinned external path -- so Validate rejected the one thing a pack daemon actually is: a binary the user built on their own Mac, which no shared manifest can carry a SHA for. The pack was accepted, the tree accepted the daemon, and then the unit it was handed could not describe itself, so snow-proxy never started and the failure surfaced only in a serve log. UnitSpec.Command closes that, joins identity() so a reattach cannot confuse two daemons, and is refused alongside a sha pin -- a pin naming a file the spec never reads is worse than no pin, because it reads as a guarantee. The consent screen renders it UNPINNED so the absence is the user's call, not a silent property. Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. prod_loc 4086 -> 4103, globals 39 -> 41: honest tri-state embed/watcher health (U4-live-embed-recall review fix). embedExercised/watcherExercised are two new package globals tracking whether a real network attempt has ever happened, closing a false-healthy gap (a fresh, unexercised embedder/watcher used to read as \"healthy\" because embedDisabled/watcherUnavailable default false). watcherHealthState is a new small function; the rest of the growth is doc comments naming a retained global-state coupling (the side-effectful live re-probe in watcherCaptureAvailable) and the deferred memWatcherWarm lazy-reintroduction note, both requested explicitly so the coupling and the deferred-not-dead status are visible at the call site, not just in a commit message. Then U5's schema-v2 upgrade: first built as a trust-state/provenance schema (admitted/proposed/quarantined, a named eligibility predicate, an automatic pre-migration snapshot), then radically simplified on final review to what the upgrade actually needs -- see docs/design/self-learning-loop.md's \"Rejected\" note. prod_loc 4103 -> 4213, globals 41 -> 43: migrateMemorySchema is a one-time DATA sweep (no new column) that adds the legacy profile column if missing, soft-deletes live rows whose historical source isn't user/cli (the store's EXISTING deleted_at mechanism, not a new trust_state column), and stamps user_version, all in one transaction. The two new globals are both consts: memSchema (unchanged shape, just no longer inline-stamps user_version) and memSchemaVersion (replacing the old bare literal 1). memKnownSources/memNormSource close the free-text `source` vocabulary, and remember() gained one spoof guard (an external caller cannot claim source=\"watcher\"; only the watcher's own internal capture path can). Rebase integration: prod_loc 4086 -> 4468, globals 39 -> 50: memory_capture adds two admission modes (explicit default, experimental-auto opt-in) gated in memory_capture_mode.go, a capture-only secret filter (memory_secretfilter.go: an op:// 1Password-locator regex plus vendor-token/high-entropy patterns) run at two points in memCapture -- before the watcher ever sees a message and again before a fact/correction is stored -- and a single persistent watcher daily budget (watcherBudgetRemaining/watcherUsedToday, a real SQLite COUNT, no session ids/maps/mutex) served by a composite idx_memories_source_created_at index and a half-open UTC-day window whose bound deliberately drops the trailing Z (RFC3339Nano trims trailing fractional zeros, so a Z-suffixed bound could lex-compare after a same-second row); both the query plan and the exact-midnight edge are covered by regression tests. rememberSourced also splits into three phases -- hash reaffirm under s.mu, the Ollama EMBED with NO lock held, then revalidate/dedupe/budget/INSERT under one final hold -- and recall now embeds its query before locking, closing a head-of-line-blocking bug where a slow embed call held under the store lock stalled every other recall and remember on the host; the phase-3 hash reaffirm is what keeps two concurrent remembers of identical content from writing two rows. embedExercised/watcherExercised give recall/capture health a tri-state so a backend that has never actually been exercised no longer reads as healthy by default. Schema migrated to v2 in one on-open, one-time data sweep (migrateMemorySchema: add the legacy profile column if missing, soft-delete live rows whose historical source predates verified provenance, stamp user_version) -- simplified down from an originally fuller trust-state/provenance design before it landed (docs/design/self-learning-loop.md's Rejected note). Net deletions: the orphaned Synthesize RPC/plugin surface (no caller outside its own tests) and the Hit.Durability read thread end to end (the DB column stays, write-only, for on-disk compatibility only); a restoreDeleted helper landed then came back out because nothing in production ever called it -- a helper that exists only to give its own test something to exercise is not coverage. Then prod_loc 4468 -> 4561: the front door stops lying about readiness. `serve` bound every listener in phase 1 but called http.Serve only after the child-spawn phase returned, so for that whole phase the kernel accepted connections into the backlog of a listener no goroutine read from -- the port was OPEN and every request HUNG, which made a TCP dial report \"up\" while the `identity` RPC reported \"unreachable\". Measured on a real host: TCP answered at 0.26s, HTTP at 15.22s (one pack daemon's preflight held the phase), against `pix serve install`'s 10s verification budget, so install ALWAYS reported a healthy daemon as unreachable. swapHandler now serves each listener from the instant it binds, behind startingMux, which answers `identity` with the right name and version plus ready=false and a reason -- so \"still starting\" is distinguishable from \"stale binary\" and from \"a foreign listener holds the port\", which a hung request collapsed into a single verdict. spawnChildren splits into spawnMemory and reconcilePacks so pack units reconcile LAST, off memory's readiness path: a pack daemon whose port a leaked orphan holds burns its entire 15s preflight budget, and used to burn it before memory could answer at all. Comments were compressed hard before raising this: the residue is one new type, one new handler, the function split, and the incident record that justifies each. A deliberate, reviewed raise of a shrink-only budget. Then prod_loc 4561 -> 4607: the follow-up to the fix above, which regressed real work while fixing probes. Answering `identity` from the instant the listener binds is right for a PROBE, but the same front door was also answering real calls \"not ready\" -- and before it served early those calls sat in an unaccepted backlog and then SUCCEEDED. uatmatrix's `remember` immediately after start went from a guaranteed pass to a coin flip, and lost on main. swapHandler now dispatches by KIND: a recognised probe is answered at once, anything else waits on a ready channel closed by set(), bounded by the request's own context exactly as the backlog bounded it. isIdentityCall does the one-time body peek that decides which, restoring r.Body so the next handler still reads it, and treats anything unrecognised as work because telling real work \"not ready\" is the failure being avoided. No new globals: the grace timer and body-limit constants both went away rather than be recorded. A deliberate, reviewed raise of a shrink-only budget. Then the candidate-owned environment-matrix seam (docs/design/self-development-uat.md; host run run-20260823-201941-8f7b648b proved the long-lived session worker still executed its own statically linked pre-candidate uatenvmatrix even after commit 74f74103 fixed fixture materialization): main.go gains one new internal `uat-env-matrix` subcommand case, intentionally absent from usageText, that dispatches to uatenvmatrix.RunCLI and os.Exits its result -- the one new os.Exit call (exits 19 -> 20) and the one new import edge into uatenvmatrix (edges 12 -> 13). prod_loc 4771 -> 4815. A deliberate, reviewed raise of a shrink-only budget."
    },
    "pix/host/cli": {
      "_prod_loc_note": "475 -> 502: Deps.Config() now warns about config keys this build ignores, so a typo is surfaced instead of silently doing nothing. A deliberate, reviewed raise of a shrink-only budget, which is the only way this number is allowed to move. Then the setup key interview: `pix setup` reported the REQUIRED `providers` gap and printed `pix models add anthropic` instead of running it, so a host with no pack could not reach a passing setup at all -- it stayed unnoticed because a managed-inference pack satisfies that row keylessly, so the gap never appeared on the hosts being exercised. The providers step now carries an Apply that delegates to that same interview through provision.Composition (provision still may not import the workflow that performs a step), gated on a terminal and not --yes. Two exports come with it: provision.ErrSkipped, so a declined prompt reports as the choice it was rather than a failed apply or a repair that never happened, and cli.AskLine, ConfirmYN's open-ended twin -- shared because it must read straight off the stream like ConfirmYN does, and a caller wrapping it in its own bufio.Reader would steal input from every later prompt. The rest is Options.Progress and health.RunWithProgress: both check rounds are concurrent under an 8s per-probe budget, so setup could sit silent for sixteen seconds, which reads as a hang. Deliberate, reviewed raises of a shrink-only budget.",
      "prod_loc": 501,
      "exports": 21,
      "globals": 2,
      "edges": 2,
      "exits": 0
    },
    "pix/host/cmd/pix": {
      "_prod_loc_note": "3036 -> 3071: `pix resume SESSION [DIR]` turns pi's exact exit receipt into a host command without exposing `run -- --session`. Then 3071 -> 3136, edges 22 -> 23: `pix reset` is back (reset_cmd.go). The new edge is cmd/pix -> workflow/reset, and it is the whole point of the shape: reset is L3 and workflow/launch is its L3 sibling, so the command layer is the ONLY place allowed to hold both. It injects `pix rm --all` into reset as reset.Sweep rather than letting reset grow a sandbox removal of its own; that alternative would have cost no edge here and created a second bulk force-removal seam beside the one explicitly-named one. Deliberate, reviewed raises of a shrink-only budget. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then 3137 -> 3158: setupProbeWrap, supplied here because the composition root is the only place allowed to know both `pack` and `mcp`. Passed as a parameter rather than a package global so a future caller cannot silently forget it and reintroduce the unwrapped probe. Then 3158 -> 3162: `pix pack use --details` was added to the pack layer's flag parser but never to the kong command, which rebuilds its own args from Target and Yes. So the launcher rejected the flag with 'unknown flag --details' and the escape hatch the summary consent screen advertises did not exist on the command line. The `d` key at the prompt did work, which is how it went unnoticed. Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Ship review: hand successful UAT registration ownership to the sandbox teardown path so kept dev sandboxes remain attachable. Then the setup key interview: `pix setup` reported the REQUIRED `providers` gap and printed `pix models add anthropic` instead of running it, so a host with no pack could not reach a passing setup at all -- it stayed unnoticed because a managed-inference pack satisfies that row keylessly, so the gap never appeared on the hosts being exercised. The providers step now carries an Apply that delegates to that same interview through provision.Composition (provision still may not import the workflow that performs a step), gated on a terminal and not --yes. Two exports come with it: provision.ErrSkipped, so a declined prompt reports as the choice it was rather than a failed apply or a repair that never happened, and cli.AskLine, ConfirmYN's open-ended twin -- shared because it must read straight off the stream like ConfirmYN does, and a caller wrapping it in its own bufio.Reader would steal input from every later prompt. The rest is Options.Progress and health.RunWithProgress: both check rounds are concurrent under an 8s per-probe budget, so setup could sit silent for sixteen seconds, which reads as a hang. Deliberate, reviewed raises of a shrink-only budget. Rebase integration: 3036 -> 3071: `pix resume SESSION [DIR]` turns pi's exact exit receipt into a host command without exposing `run -- --session`. Then 3071 -> 3136, edges 22 -> 23: `pix reset` is back (reset_cmd.go). The new edge is cmd/pix -> workflow/reset, and it is the whole point of the shape: reset is L3 and workflow/launch is its L3 sibling, so the command layer is the ONLY place allowed to hold both. It injects `pix rm --all` into reset as reset.Sweep rather than letting reset grow a sandbox removal of its own; that alternative would have cost no edge here and created a second bulk force-removal seam beside the one explicitly-named one. Deliberate, reviewed raises of a shrink-only budget. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then 3137 -> 3158: setupProbeWrap, supplied here because the composition root is the only place allowed to know both `pack` and `mcp`. Passed as a parameter rather than a package global so a future caller cannot silently forget it and reintroduce the unwrapped probe. Then 3158 -> 3162: `pix pack use --details` was added to the pack layer's flag parser but never to the kong command, which rebuilds its own args from Target and Yes. So the launcher rejected the flag with 'unknown flag --details' and the escape hatch the summary consent screen advertises did not exist on the command line. The `d` key at the prompt did work, which is how it went unnoticed. Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Ship review: hand successful UAT registration ownership to the sandbox teardown path so kept dev sandboxes remain attachable. Then the setup key interview: `pix setup` reported the REQUIRED `providers` gap and printed `pix models add anthropic` instead of running it, so a host with no pack could not reach a passing setup at all -- it stayed unnoticed because a managed-inference pack satisfies that row keylessly, so the gap never appeared on the hosts being exercised. The providers step now carries an Apply that delegates to that same interview through provision.Composition (provision still may not import the workflow that performs a step), gated on a terminal and not --yes. Two exports come with it: provision.ErrSkipped, so a declined prompt reports as the choice it was rather than a failed apply or a repair that never happened, and cli.AskLine, ConfirmYN's open-ended twin -- shared because it must read straight off the stream like ConfirmYN does, and a caller wrapping it in its own bufio.Reader would steal input from every later prompt. The rest is Options.Progress and health.RunWithProgress: both check rounds are concurrent under an 8s per-probe budget, so setup could sit silent for sixteen seconds, which reads as a hang. Deliberate, reviewed raises of a shrink-only budget. Integration 3368 -> 3374: memory command startup diagnostics use the dedicated bounded 3s wait and distinguish a daemon still starting from one that is down; reviewed U3 lifecycle raise. Phase-10 memory closeout 3374 -> 3387: `pix memory --help` now states the capture-mode mental model (what explicit vs experimental-auto actually store, that turning capture off binds already-running sandboxes, and that `/forget` is the undo) instead of leaving it to be discovered from the docs. A deliberate, reviewed raise. Then prod_loc 3387 -> 3404: an explicit --dev attach is no longer treated as an ignorable create-only hint. The command layer requires the existing sandbox's UAT registration before attaching, so a session cannot claim dev intent while omitting uat_capabilities; `pix --dev` is the TTY-gated shorthand for the same path. A deliberate, cross-vendor-reviewed raise. Then U2 of the self-development UAT auth repair (docs/design/self-development-uat.md): uat_worker_lifecycle.go's ensureUatWorkerOrFail wires uat.EnsureWorker into both the create path (right after RegisterMCP creates the secure session dir, before RunSession can let a gateway relay need it) and the --dev attach path (right after ResolveAttachRegistration, before session attach), rolling the create path's registration/MCP back on a worker-start failure exactly like every other create-time UAT failure already does. This is also where WorkerSpawn's stdout/stderr writers are wired to os.Stderr, since cmd/pix is the only layer allowed to name the process boundary (processboundary_test.go). prod_loc 3404 -> 3476. A deliberate, reviewed raise of a shrink-only budget. Then E1.1 (docs/design/environments.md section 5.6, AC-20): `pix run`'s half of the native-environments sbx version gate. gateSbxVersion runs health.SbxProbe once, bounded by health.StatusBudget, BEFORE any config load or sandbox side effect, and refuses with the byte-exact section 5.6 copy on a too-old or unparsable sbx version; a missing, denied, or otherwise unreachable sbx is left to the existing, separate remedies (unloadedLocalImage, the \"exec sbx\" failure's SbxInstallHint) rather than being reinterpreted as a version problem. No new exported surface: gateSbxVersion is unexported. prod_loc 3476 -> 3504. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 3504,
      "exports": 15,
      "globals": 9,
      "edges": 25,
      "exits": 1
    },
    "pix/host/config": {
      "_prod_loc_note": "prod_loc 796 -> 804: ServiceEnabled now resolves `services` the way `pix-host serve` does, and the load-bearing half is that EMPTY MEANS ALL (resolveServices: \"an empty result means all\"). Reading empty as none made doctor call memory OPTIONAL on a config where serve starts it -- the commonest config there is -- so a memory unit that was genuinely down could not fail readiness. A deliberate, reviewed raise. Rebase integration: prod_loc 796 -> 804: ServiceEnabled now resolves `services` the way `pix-host serve` does, and the load-bearing half is that EMPTY MEANS ALL (resolveServices: \"an empty result means all\"). Reading empty as none made doctor call memory OPTIONAL on a config where serve starts it -- the commonest config there is -- so a memory unit that was genuinely down could not fail readiness. A deliberate, reviewed raise. Then memory_capture: one new omitempty string field, its default/validate/sparse-save triple (ValidMemoryCapture, MemoryCaptureModes, DefaultMemoryCapture), following the exact pattern memory_watcher_model/run_intent already established, with only TWO modes (explicit|experimental-auto, no review/staging mode). prod_loc 804 -> 838, exports 40 -> 42 (MemoryCaptureExplicit/ExperimentalAuto consts plus MemoryCaptureModes/ValidMemoryCapture), globals 13 -> 14. A deliberate, reviewed raise of a shrink-only budget. Then E1.5 (Story 1, native sandbox environments, docs/design/environments.md §5.3): the machine-config half of `environment = \"NAME\"` / `[environments]` name -> canonical absolute path. Config gained Environment/Environments plus environment.go's CanonicalEnvironmentPath/AddEnvironment/RemoveEnvironment/UseEnvironment (registration accepts a leading ~, only a canonical absolute path is ever persisted) and applyDefaults' dropNoncanonicalEnvironments, which fails closed on a hand-edited noncanonical entry the same way a retired [plugins.*] slot is surfaced. `pix env` (Wave C) is the only writer; `pix config set/unset` refuses both keys outright (see pix/host/workflow/provision's note). prod_loc 838 -> 1017, exports 42 -> 43 (CanonicalEnvironmentPath). A deliberate, reviewed raise of a shrink-only budget. Then E1.5 pre-merge fix: environmentKeyRefusal now also refuses the exact `environments` key (no `.<name>` suffix) instead of falling through to the generic unknown-key path, and AddEnvironment rejects an empty/whitespace path instead of silently canonicalizing it to CWD. prod_loc 1017 -> 1027. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 1027,
      "exports": 43,
      "globals": 14,
      "edges": 0,
      "exits": 0
    },
    "pix/host/envinfo": {
      "_prod_loc_note": "Wave B integration merge of E1.2 (native `.sbxenv.yaml` parser, parse.go/tree.go/merge.go/interpolation.go) and E1.3 (strict pix.toml sidecar parser, sidecar.go), landed on parallel unit branches against the same new package. E1.2: 795 -> 860, exports 36 -> 37, globals 7 -> 8: pre-merge finding fixes. (1) BuildTree now scans mcp.servers[].args for authored `${VAR}` interpolation (it previously scanned only url/command), plus the audit's two other real gaps on fields BuildTree already exposed as nodes but never scanned: a kit's raw authored path (kits[i]) and a binding's destination domain (bindings.<svc>.apiKey.domains[<domain>]). (2) BuildTree now refuses a duplicate domain within one service's apiKey.domains as a stable-identity collision (ErrDuplicateIdentity, reusing the existing sentinel rather than growing a second one) instead of silently emitting two BindingDomainNode entries sharing one key path -- covering both a duplicate authored directly in one file and one introduced only by merge.go's documented list-concatenation across files. (3) the one new export and new global, ErrEmptyBindingDomains: a binding whose effective, post-merge domain list is empty no longer evaporates from the tree with no trace -- BuildTree refuses it by name (`bindings.<svc>.apiKey.domains`) rather than inventing a zero-domain tree node upstream gives no meaning to (docs/upstream/sbx-0.37-binding-warning.md already shows sbx itself treating a zero-domain binding as a functionless no-op: \"no domains allowed by your bindings; not injecting\"). E1.3 (docs/design/environments.md section 5.2): the strict pix.toml sidecar parser, self-contained on unit/native-e1-3 while E1.2 laid out the rest of the package in parallel; TestArchitecture_ImportsPointDown's pkgLayer placement was E1.2's to add on merge. Pre-merge finding fix: prod_loc 409 -> 463, globals 4 -> 2. The regex-based line-scanner (scanFile/schemaNode/newSchema/check, three package-level regexps) only recognized bare unquoted keys, so a quoted unknown or native-owned key (e.g. \"typo_field\" = \"oops\") never matched and sailed straight through to the final toml.Decode, which does not error on fields a target struct doesn't declare -- a silent strict-parsing bypass. The naive splitDotted also split a quoted dotted identity like host.mcp.\"github.copilot\" into two segments at the embedded dot, wrongly rejecting a single legitimate identity. Both are fixed by deriving accept/reject from the real decoder's own metadata (toml.MetaData.Undecoded on the same Decode call that already produces Sidecar) instead of a hand-rolled schema tree: the Sidecar struct's toml tags already ARE the schema, and Undecoded's keys already resolve TOML's quoting rules, so a quoted key can no longer be missed and a quoted dotted segment can no longer be split. The former scanner is now locateKeyLines, a quote-aware, cosmetic-only line-number lookup for diagnostics -- it can never affect the accept/reject decision, so an imperfect match there costs a line number, never a validation bypass. Integration fix at merge time: both branches independently exported `Parse(path string)` over their own document type (E1.2's *Document from .sbxenv.yaml, E1.3's *Sidecar from pix.toml) in the same package, an unresolvable duplicate declaration. The native `.sbxenv.yaml` parser keeps the established `Parse` (parse.go, ParseBytes's sibling, the package's original Story-1 entry point); the sidecar parser is renamed `ParseSidecar` end to end (sidecar.go, sidecar_test.go's 19 call sites) so the two strict, differently-scoped parsers are never ambiguous at a call site. Both strict behaviors are unchanged and fully covered: parse_test.go's suite for `Parse`/`ParseBytes` and sidecar_test.go's suite for `ParseSidecar` both pass unmodified aside from the rename. Values below are freshly measured on the merged package (`go run ./scripts/arch-metrics -root services/host`) after the rename, not the higher of the two pre-merge branch numbers: prod_loc 795+409 branch-local -> 1327 merged (parse/tree/merge/interpolation + sidecar + doc/errors/document scaffolding, no double-count), exports 36+14 -> 51 (both parsers' full public surface, including the renamed ParseSidecar), globals 7+2 -> 10, edges 0, exits 0.",
      "prod_loc": 1327,
      "exports": 51,
      "globals": 10,
      "edges": 0,
      "exits": 0
    },
    "pix/host/health": {
      "_prod_loc_note": "prod_loc 1277 -> 1286, globals 29 -> 30: the probe half of McpAuthNotRequired (see pix/host/mcp's note) -- a server that needs no OAuth is now reported as ANSWERED rather than counted against the row as unproven, which is what made the mcp row a permanent `?`. Carried with the earlier raise in this package: prod_loc 1254 -> 1277: the mcp row's evidence was ONE run-on line -- eight servers, each with its own parenthetical, joined into several hundred characters that no one can read in a terminal or skim anywhere. A reader could not tell how many servers there were, which one was theirs, or where one note ended. writeEvidence now gives a set-valued evidence string one line per item under a count, and the notes are joined with newlines because \"; \" cannot be the separator: the notes contain semicolons, and splitting on those turned 8 servers into 16 half-sentences. Single-item evidence is untouched, so every other row reads exactly as before. A deliberate, reviewed raise of a shrink-only budget. Then prod_loc 1286 -> 1456, globals 30 -> 34: doctor stopped treating \"registered with the gateway\" as \"working\". MCPServer gained Undeclared/Command/Probe and a LookPath seam, and checkServer gained four real questions it never asked: does the declared command still resolve, does any active pack still declare this name, does the server's own probe pass, and did the probe actually get to run. Per-server checks also became concurrent, because the budget is per-probe and one `op run` blocking on a locked vault used to starve every check after it and report gaps that were never measured. This is the change: the previous cheapness WAS the bug — an eight-server host read `✓ 8 registered` while two of those registrations pointed at commands that no longer existed. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then 1549 -> 1557: gap and unknown remedies are APPENDED rather than one falling back to the other. A host with both (a locked vault plus an expired grant — the state of a fresh laptop) was shown only the gaps' fix, leaving the majority of its servers with no way to act. Then the review round: `pix secret check` was UNBOUNDED, so a locked 1Password vault (the normal state of a fresh laptop) hung it forever after printing FAIL for references that were perfectly correct, sending users to re-check something that was fine. It is bounded now, with 'could not check' as a third answer, and the stale-key line offers removal instead of advice that leads nowhere. Then the first REAL pix setup run, which exposed what four review rounds could not because no agent was allowed to run that verb: the credential solicitor skipped every integration with a setup hook -- which is every integration that needs a credential -- so nothing was ever asked for and setup died on the first missing reference having offered no way to supply it. Credentials are now solicited BEFORE registration too, because registering first produced a bare, unwrapped server on a clean machine: exactly the broken state this whole surface exists to prevent. Requirements report all at once with pack-authored guidance, and a failed setup phase can no longer be headlined ready. Then the daemon runtime: a third [[services]] runtime for a long-running HOST process that already speaks its own protocol on a loopback port. It exists because go-plugin is the wrong shape for one -- snow-proxy is reached over HTTP by an in-sandbox wrapper, so a net/rpc handshake would replace a working transport to gain nothing -- and because the Port/Listen/Health fields on Service were designed for exactly this and had no runtime that read them. It reuses everything that matters (staged copy-then-verify, env allowlist, restart budgets, the permanent-vs-transient split, and the rule that a running process is not health) and adds only launch + probe. The doctor row is the point: a pack daemon was the one host-exec facet with NO row, so when snow-proxy died the warehouse capability degraded in total silence. Then the github credential row. A sandbox holds no GitHub credential of its own, so an agent commits, finishes, tries to push, and only then reports it cannot -- which is exactly what happened. Nothing in setup or doctor mentioned a token was needed. Worse, the one place that DID report it lied: host state greps `sbx secret ls` for the word github, and that listing includes SANDBOX-SCOPED secrets, so a credential pinned to one box read as available to all. The agent believed it. Scope is now asked with sbx's own --global filter rather than by parsing a SCOPE column whose formatting is not ours to depend on, and the four answers stay distinct: global is ready, scoped is a gap that NAMES the sandbox and says a later one gets nothing, absent is a gap, and sbx not answering is unknown with no repair invented. Then 1804 -> 1844: the launchd row stopped confusing LOADED with ABLE TO START. `pix serve install` resolved the binary through its symlink, so a Homebrew install wrote a versioned Cellar path into the plist and the next upgrade deleted it — launchd keeps that job and reports it cheerfully, while every `launchctl kickstart -k` blocks on a job that can never spawn. A user spent three days on 'pix setup hangs' with this row printing a green `agent loaded` the whole time. It now parses `program =` out of the same launchctl output it already had and checks the file is there; launchctl saying nothing about a program is still READY, because silence is not evidence of absence. A deliberate, reviewed raise of a shrink-only budget. Then 1844 -> 1933, exports 47 -> 48: the fifth health status, StatusOff (\"verified, optional, and intentionally not configured\"). Before it existed, an optional probe with nothing to check had exactly two ways to lie: PackProbe handed out `pix pack use` as a Fix for a host that never wanted a pack (a false gap, filed under \"missing\", degrading the headline), while MemoryUnitProbe (disabled), MCPProbe and DaemonProbe (empty sets), and provision's ollama models probe (zero tags) all claimed StatusReady for a capability nothing was actually verified about. StatusOff is its own answer: Missing() and OK() are both false, it never reaches Gaps()/OptionalGaps()/Fixes()/Blocking(), and the runner defensively downgrades a Required() probe that reports it (a probe bug, never trusted). The added export is the constant plus Result.Hint, the non-repair invitation text doctor renders indented under an off row instead of folding it into \"Fix:\". A deliberate, reviewed raise of a shrink-only budget. Rebase integration: edges 3 -> 4 (still current, U3): MemoryUnitProbe compares the memory daemon's reported identity version against launcher.Version to surface a stale/mismatched build, read-only, no restart -- the reason this package imports pix/host/launcher at all. U9 then deleted MemoryUnitProbe.WantVersion, a test-only override seam for that comparison that no production caller ever set (along with its one test case); the version comparison itself, and the edge it requires, were not touched and stayed exactly as U3 left them. A deliberate, reviewed raise of a shrink-only budget. Then E1.1 (docs/design/environments.md section 5.6, AC-20): the native-environments sbx version gate, layered ON TOP of SbxProbe's existing exec classification rather than folded into it — a broken/timed-out/denied sbx keeps classifying exactly as before, and TestSbxProbe_RealExecutableOutcomes is the unmodified oracle for that. New: SbxMinVersion (\"0.39.0\"), SbxUpgradeFix, sbxUnparsableDetail, sbxVersionAtLeast/sbxVersionParts (a numeric, not lexical, dotted-version compare — \"0.9\" vs \"0.10\" would get it backwards lexically), and the exported SbxVersionGate/SbxVersionGateMessage pair `pix run` and `pix doctor` both call so the byte-exact PRD copy cannot drift between the two surfaces. versionish now captures a full x.y.z (it previously matched only x.y, silently truncating \"0.38.2\" to \"0.38\"), needed so the gate's \"found:\" line names the actual observed version. prod_loc 1956 -> 2058, exports 49 -> 53 (the four exported names above), globals 39 -> 42 (SbxMinVersion, SbxUpgradeFix, sbxUnparsableDetail). A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 2058,
      "exports": 53,
      "globals": 42,
      "edges": 4,
      "exits": 0
    },
    "pix/host/hostenv": {
      "prod_loc": 46,
      "exports": 3,
      "globals": 0,
      "edges": 2,
      "exits": 0
    },
    "pix/host/hosttrust": {
      "_prod_loc_note": "Initial budget. E1.4: the launcher-owned host-exec trust mechanism (canonical identity, the JSON-then-sha256 fingerprint engine, the one Record/AcceptanceStore shape keyed by opaque Subject{Kind,Root}, flock-serialized fresh-load->mutate->save, symlink-refused atomic document I/O, and content hashing) extracted from workflow/pack's trust.go/truststore.go, which shrank by exactly what moved here (see that package's own note). No behavior changed: workflow/pack's fingerprints, file paths, and JSON shapes are byte-identical before and after (trust_fingerprint_compat_test.go). L1 capability, no sibling import.",
      "prod_loc": 288,
      "exports": 13,
      "globals": 0,
      "edges": 1,
      "exits": 0
    },
    "pix/host/inference": {
      "_prod_loc_note": "raised together for ONE change: `pix doctor` and `pix setup` demanded an anthropic/openai/google key from hosts whose models need none. A pack's sbx-session backends carry their own credential (a Docker login session the sbx proxy injects inside the sandbox, unreplayable from the host by design), so the key store lists nothing and the required row went red prescribing `pix models add anthropic` -- a key nothing would read. `pix run`'s launch gate already skipped the key check on exactly this host (inference.ConfiguredKeylessInference); ProviderKeyProbe.Keyless + inference.KeylessBackends give the same carve-out to the two verbs that lacked it, so all three stop disagreeing about one host. Distinct from Callable, which answers \"this key routes nowhere\"; Keyless answers \"no key was ever the credential\". provision's new edge is importing inference for that one predicate rather than re-deriving a second opinion. A deliberate, reviewed raise of a shrink-only budget.",
      "_prod_loc_note2": "936 -> 953: manifestModels dropped every inference binding the shipped registry did not know, so an Ollama model the user pulled outside the catalog was bound by setup and then stripped before it reached inference.json or CallableRuntimeModels. The result was a feature that appeared to work and could never be called: `pix run --model ollama/<tag>` failed with \"not available through the configured inference backends\". Carrying an unknown-but-bound model through the manifest needs a synthesized identity that asserts no price and no context window, and that never leaks into routing. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 953,
      "exports": 33,
      "globals": 8,
      "edges": 3,
      "exits": 0
    },
    "pix/host/launcher": {
      "prod_loc": 63,
      "exports": 4,
      "globals": 2,
      "edges": 0,
      "exits": 0
    },
    "pix/host/lease": {
      "prod_loc": 768,
      "exports": 19,
      "globals": 12,
      "edges": 0,
      "exits": 0,
      "_prod_loc_note": "Then 736 -> 768, exports 18 -> 19: ReferencesHeld, the READ-ONLY form of the question teardown asks. `pix ls` could not say why a sandbox was still there, so six surviving boxes read as 'teardown is broken' when four of them simply had shells that had never exited — the evidence sat in a journal nobody reads and a lock nobody could see. It answers from the LOCK, never a pid, because doc.go is explicit that a pid is reused the instant its owner exits. A deliberate, reviewed raise of a shrink-only budget."
    },
    "pix/host/mcp": {
      "_prod_loc_note": "prod_loc 1033 -> 1050, exports 28 -> 29, globals 18 -> 19: ONE outcome, McpAuthNotRequired. `sbx mcp auth status` answers \"does not require OAuth\" for a server whose credentials come from the gateway's op-refs injection rather than an OAuth broker (slack, google-docs-create). The parser had buckets only for authorized and failed, so a DEFINITE answer fell through to unknown and `pix doctor` carried a permanent `?` that no action could ever clear -- which teaches a reader to stop reading the glyph, the same way a fix that cannot fix anything teaches them to stop reading fixes. The new case is matched BEFORE the negatives, because \"does not require oauth\" contains \"not\". A deliberate, reviewed raise of a shrink-only budget. Then the review round: `pix secret check` was UNBOUNDED, so a locked 1Password vault (the normal state of a fresh laptop) hung it forever after printing FAIL for references that were perfectly correct, sending users to re-check something that was fine. It is bounded now, with 'could not check' as a third answer, and the stale-key line offers removal instead of advice that leads nowhere. Then the review round: `pix secret check` was UNBOUNDED, so a locked 1Password vault (the normal state of a fresh laptop) hung it forever after printing FAIL for references that were perfectly correct, sending users to re-check something that was fine. It is bounded now, with 'could not check' as a third answer, and the stale-key line offers removal instead of advice that leads nowhere.",
      "prod_loc": 1081,
      "exports": 30,
      "globals": 19,
      "edges": 4,
      "exits": 0
    },
    "pix/host/memory": {
      "_prod_loc_note": "Then 206 -> 185: `stats`'s plain-text line drops the durable/perishable split now that every row the host writes is durable; --json stays a raw passthrough, untouched. A shrink-only ratchet. Rebase integration: QA-2: prod_loc 189 -> 199. `pix memory forget <missing>` used to print its miss to Out and return nil -- exit 0 with the diagnostic on stdout, indistinguishable from a real deletion by exit code or stream. Forget now returns a plain error on a miss (dispatch's existing 'pix: %v' render puts it on stderr and maps it to exit 1, no new plumbing), while --json still writes the {\"ok\":false} result to Out so a script piping --json never has to special-case a miss to get valid JSON. A deliberate, reviewed raise of a shrink-only budget. Then Phase-10 memory closeout 199 -> 209: `stats` labels the watcher's rule rows \"corrections\" (what they are) rather than \"learnings\" (vague, and easily read as facts), keeping the wire key `learnings` untouched for --json/RPC consumers, and memoryMeta's separator now matches the sandbox's own /recall render so one row does not wear two punctuations across two surfaces. A deliberate, reviewed raise. Then DX-6a: prod_loc 209 -> 224, the render-only correction alias. memoryMeta's kind segment now goes through a new displayKind(kind string) string helper that maps the stored \"learning\" kind to \"correction\" for display and passes every other kind through unchanged; the stored row and --json output still carry \"learning\" verbatim, no schema migration. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 224,
      "exports": 1,
      "globals": 0,
      "edges": 2,
      "exits": 0
    },
    "pix/host/packinfo": {
      "prod_loc": 1387,
      "exports": 38,
      "globals": 7,
      "edges": 4,
      "exits": 0,
      "_prod_loc_note": "prod_loc 942 -> 1215, exports 32 -> 37, globals 4 -> 6: the pack manifest grew the two things that make a pack checkable. Integrations gained a transport (command/args/probe alongside image/manifest/url) so every declared server names something that can actually be started, and setup steps gained a declarative require/apply vocabulary with fail-closed validation. Most of the added lines are that validation. It buys the defect class outright: the previous shell hooks called `pix` verbs that had been deleted, so a pack's setup could never pass, and nothing detected it until a user tried to onboard. A pack that names no commands cannot be broken by a CLI change. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then the review round: `pix secret check` was UNBOUNDED, so a locked 1Password vault (the normal state of a fresh laptop) hung it forever after printing FAIL for references that were perfectly correct, sending users to re-check something that was fine. It is bounded now, with 'could not check' as a third answer, and the stale-key line offers removal instead of advice that leads nowhere. Then the first REAL pix setup run, which exposed what four review rounds could not because no agent was allowed to run that verb: the credential solicitor skipped every integration with a setup hook -- which is every integration that needs a credential -- so nothing was ever asked for and setup died on the first missing reference having offered no way to supply it. Credentials are now solicited BEFORE registration too, because registering first produced a bare, unwrapped server on a clean machine: exactly the broken state this whole surface exists to prevent. Requirements report all at once with pack-authored guidance, and a failed setup phase can no longer be headlined ready. Then the daemon runtime: a third [[services]] runtime for a long-running HOST process that already speaks its own protocol on a loopback port. It exists because go-plugin is the wrong shape for one -- snow-proxy is reached over HTTP by an in-sandbox wrapper, so a net/rpc handshake would replace a working transport to gain nothing -- and because the Port/Listen/Health fields on Service were designed for exactly this and had no runtime that read them. It reuses everything that matters (staged copy-then-verify, env allowlist, restart budgets, the permanent-vs-transient split, and the rule that a running process is not health) and adds only launch + probe. The doctor row is the point: a pack daemon was the one host-exec facet with NO row, so when snow-proxy died the warehouse capability degraded in total silence. Then the third launch form. UnitSpec described only two ways to start a unit -- self-exec or a SHA-pinned external path -- so Validate rejected the one thing a pack daemon actually is: a binary the user built on their own Mac, which no shared manifest can carry a SHA for. The pack was accepted, the tree accepted the daemon, and then the unit it was handed could not describe itself, so snow-proxy never started and the failure surfaced only in a serve log. UnitSpec.Command closes that, joins identity() so a reattach cannot confuse two daemons, and is refused alongside a sha pin -- a pin naming a file the spec never reads is worse than no pin, because it reads as a guarantee. The consent screen renders it UNPINNED so the absence is the user's call, not a silent property."
    },
    "pix/host/plugin": {
      "_prod_loc_note": "prod_loc 388 -> 394: RememberReq drops Reward (alongside the already-gone Durability/TTLDays) since reward is no longer write-path input at all, and SynthesizeResp drops Expired now that the TTL-expiry sweep it reported on had no remaining caller. Both changes are explained inline (why the field is gone, what the column/response still does), which is most of the added lines; no exported surface, global, or edge count moved. Then 394 -> 360, exports 23 -> 20: Promotable/PromotableReq/PromotableResp/PromotableCandidate and the net/rpc server+client stubs are deleted end to end now that `pix memory learnings`, `/learnings`, and the `promotable` RPC have no remaining caller. Health.Vector/Capture then become *bool (tri-state: nil = not yet exercised), adding six production lines without changing the exported surface, globals, or edges.",
      "prod_loc": 366,
      "exports": 20,
      "globals": 5,
      "edges": 0,
      "exits": 0
    },
    "pix/host/recreatelog": {
      "_prod_loc_note": "E1.6: initial budget for the local-only, bounded (MaxRecords=100) environment recreate-boundary diagnostic log (docs/design/environments.md section 10.2). Two files, recreatelog.go + doc.go: Record{Timestamp,Environment,ChangedKeyPaths}, canonicalization/validation (reject empty/absolute/traversal/control-char changed key paths, reject invalid environment names), an flock-guarded append (dedicated lock file, distinct from the atomically-swapped data file) and a strict DisallowUnknownFields reader that fails closed on malformed or foreign-written content. Zero internal imports by construction (F10, guard_test.go), so it satisfies the L1 down-only/no-sibling rule without help. Not wired into doctor or any workflow yet (deferred to Story 2).",
      "prod_loc": 301,
      "exports": 4,
      "globals": 5,
      "edges": 0,
      "exits": 0
    },
    "pix/host/routing": {
      "prod_loc": 939,
      "exports": 29,
      "globals": 5,
      "edges": 0,
      "exits": 0
    },
    "pix/host/rpc": {
      "prod_loc": 201,
      "exports": 12,
      "globals": 6,
      "edges": 0,
      "exits": 0
    },
    "pix/host/sandbox": {
      "prod_loc": 871,
      "exports": 27,
      "globals": 23,
      "edges": 0,
      "exits": 0
    },
    "pix/host/secret": {
      "_prod_loc_note": "1240 -> 1290, exports 42 -> 43, globals 8 -> 9: web-search TOOL keys (ToolKeyRefOrder) are now seeded/checked/mirrored like model-provider keys, but held OUT of ProviderKeyRefOrder and ModelProviders so `pix models add` never offers a name it would reject and a missing search key can never refuse a launch. The new export is the list itself; the new global is the sync map, now DERIVED from both lists so an entry cannot be added to one and forgotten in the other. A deliberate, reviewed raise of a shrink-only budget. Then prod_loc 1290 -> 1306, exports 43 -> 44: the non-secret allowlist became a parameter (secret.NonSecret) instead of a hardcoded map of one vendor's three GOG_* variables. Pix now allowlists nothing of its own; the set is exactly what the active pack declares as env_keys, so a pack that needs a plain-text variable is the thing that authorizes it and deactivating that pack takes its allowances with it. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then the review round: `pix secret check` was UNBOUNDED, so a locked 1Password vault (the normal state of a fresh laptop) hung it forever after printing FAIL for references that were perfectly correct, sending users to re-check something that was fine. It is bounded now, with 'could not check' as a third answer, and the stale-key line offers removal instead of advice that leads nowhere. Then the github credential row. A sandbox holds no GitHub credential of its own, so an agent commits, finishes, tries to push, and only then reports it cannot -- which is exactly what happened. Nothing in setup or doctor mentioned a token was needed. Worse, the one place that DID report it lied: host state greps `sbx secret ls` for the word github, and that listing includes SANDBOX-SCOPED secrets, so a credential pinned to one box read as available to all. The agent believed it. Scope is now asked with sbx's own --global filter rather than by parsing a SCOPE column whose formatting is not ours to depend on, and the four answers stay distinct: global is ready, scoped is a gap that NAMES the sandbox and says a later one gets nothing, absent is a gap, and sbx not answering is unknown with no repair invented.",
      "prod_loc": 1449,
      "exports": 51,
      "globals": 14,
      "edges": 3,
      "exits": 0
    },
    "pix/host/service": {
      "_prod_loc_note": "1664 -> 1708: `serve stop` now sweeps for orphaned `pix-host serve` processes AFTER stopping the pid its pidfile recorded. A pidfile names one daemon and is not proof there is only one, so a second daemon outliving its own pidfile kept holding :11435 while stop reported success. That is why `pix serve stop` had to be run twice. The sweep only ever signals pids that positively verify as our live serve. A deliberate, reviewed raise of a shrink-only budget. Then reset's post-stop settle: a launchd boot-out returns when the unit is unloaded, not when the process has exited, and this daemon closes a sqlite writer and reaps a supervised child on the way out. Two seconds was not enough on a real machine, so reset refused to move the data directory and left a HALF reset (config moved aside, data and state kept, every MCP registration removed) for a pid that was gone moments later. Then 1722 -> 1724: `pix serve` help described only memory, though serve now supervises the daemons a pack declares in [[services]]. Help that undersells what a command does is the same defect as help that oversells it -- a user onboarding today cannot find out that their pack's daemon is supervised here. `serve status` was verified to actually list those units before the help was allowed to claim it does. prod_loc 1724 -> 1748, globals 16 -> 17: every service-control child is bounded. `launchctl kickstart -k` kills the running daemon and WAITS for it to die, so a `pix-host serve` wedged in shutdown blocked the CLI forever inside propagateConfig — which already had the right answer for a restart that fails and never got to give it. WaitDelay is half the fix: without it, killing the child still leaves CombinedOutput blocked on a pipe an inherited grandchild holds open. A deliberate, reviewed raise of a shrink-only budget. Then the daemon restart announces itself BEFORE it blocks. kickManaged kills the daemon and waits for it to die, and the only message was on the far side of that wait — so a command that had narrated every other step went silent for its whole budget at the very end, and a real `pix setup` was Ctrl-C'd there by a user who read it as a hang. Both paths say it now, success and failure, and the failure path is where the silence was longest. Then 1755 -> 1768: the supervised binary path keeps its symlink. Resolving it baked Homebrew's VERSIONED Cellar path into the plist, which the next `brew upgrade` deletes — leaving launchd with a job it can never spawn (exit 78 EX_CONFIG, parked in `spawn scheduled`) and `launchctl kickstart -k`, which pix runs after every pack change, blocking forever on it. Three 'pix setup hangs' on a real host traced to one stale plist. The growth is the comment and the guard, not the logic: EvalSymlinks came OUT. A deliberate, reviewed raise of a shrink-only budget. Rebase integration: 1664 -> 1708: `serve stop` now sweeps for orphaned `pix-host serve` processes AFTER stopping the pid its pidfile recorded. A pidfile names one daemon and is not proof there is only one, so a second daemon outliving its own pidfile kept holding :11435 while stop reported success. That is why `pix serve stop` had to be run twice. The sweep only ever signals pids that positively verify as our live serve. A deliberate, reviewed raise of a shrink-only budget. Then reset's post-stop settle: a launchd boot-out returns when the unit is unloaded, not when the process has exited, and this daemon closes a sqlite writer and reaps a supervised child on the way out. Two seconds was not enough on a real machine, so reset refused to move the data directory and left a HALF reset (config moved aside, data and state kept, every MCP registration removed) for a pid that was gone moments later. Then 1722 -> 1724: `pix serve` help described only memory, though serve now supervises the daemons a pack declares in [[services]]. Help that undersells what a command does is the same defect as help that oversells it -- a user onboarding today cannot find out that their pack's daemon is supervised here. `serve status` was verified to actually list those units before the help was allowed to claim it does. prod_loc 1724 -> 1748, globals 16 -> 17: every service-control child is bounded. `launchctl kickstart -k` kills the running daemon and WAITS for it to die, so a `pix-host serve` wedged in shutdown blocked the CLI forever inside propagateConfig — which already had the right answer for a restart that fails and never got to give it. WaitDelay is half the fix: without it, killing the child still leaves CombinedOutput blocked on a pipe an inherited grandchild holds open. A deliberate, reviewed raise of a shrink-only budget. Then the daemon restart announces itself BEFORE it blocks. kickManaged kills the daemon and waits for it to die, and the only message was on the far side of that wait — so a command that had narrated every other step went silent for its whole budget at the very end, and a real `pix setup` was Ctrl-C'd there by a user who read it as a hang. Both paths say it now, success and failure, and the failure path is where the silence was longest. Then 1755 -> 1768: the supervised binary path keeps its symlink. Resolving it baked Homebrew's VERSIONED Cellar path into the plist, which the next `brew upgrade` deletes — leaving launchd with a job it can never spawn (exit 78 EX_CONFIG, parked in `spawn scheduled`) and `launchctl kickstart -k`, which pix runs after every pack change, blocking forever on it. Three 'pix setup hangs' on a real host traced to one stale plist. The growth is the comment and the guard, not the logic: EvalSymlinks came OUT. A deliberate, reviewed raise of a shrink-only budget. Integration 1768 -> 1878, exports 24 -> 25, globals 17 -> 19: U3 deletes the read-side stale restart state machine and replaces it with bounded identity verification on explicit service install/start; most growth is regression coverage seams and comments, reviewed lifecycle raise. Then memory_capture joins the daemon-affecting key set (one map entry): changing it requires the same serve-restart propagation memory_watcher_model already triggers, since the daemon reads it once at startup via MEMORY_CAPTURE_MODE. prod_loc 1878 -> 1879. A deliberate, reviewed raise of a shrink-only budget. Then prod_loc 1879 -> 1921: managed stop now waits for its pre-bootout verified pid to terminate before releasing launchd's success wording, and clears stale pid/lazy state only after convergence. Unix zombie detection closes the gap where kill(pid, 0) succeeds after argv identity disappears, without weakening the PID-reuse re-verification gate or signalling a managed process directly. This fixes an observed immediate stop/start race that otherwise stranded the old memory listener. A deliberate, cross-vendor-reviewed raise of a shrink-only budget. Then prod_loc 1921 -> 1934: the generated LaunchAgent carries ExitTimeOut 30, and install's kickstart drops -k. launchd's default 5s exit timeout is SHORTER than serve's own shutdown budget (5s front-door drain, then supervise's 15s Stop), so every bootout and `kickstart -k` SIGKILLed pix-host mid-teardown -- and its pack daemons, deliberately in their own process group so a vendor CLI cannot outlive them, survived reparented to launchd with their loopback ports still bound. One such orphan is permanent and makes every later start burn the full preflight budget before failing that daemon for good; found one on a real host, PPID 1, six days old. Dropping -k on the install path removes a redundant kill of the process bootstrap+RunAtLoad had just started, which measured ~7 of `pix serve install`'s 10.7s. launchdRestart keeps -k, where replacing the running process is the entire point. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 1934,
      "exports": 25,
      "globals": 19,
      "edges": 6,
      "exits": 0
    },
    "pix/host/supervise": {
      "prod_loc": 1971,
      "exports": 40,
      "globals": 30,
      "edges": 1,
      "exits": 0,
      "_prod_loc_note": "Then the daemon runtime: a third [[services]] runtime for a long-running HOST process that already speaks its own protocol on a loopback port. It exists because go-plugin is the wrong shape for one -- snow-proxy is reached over HTTP by an in-sandbox wrapper, so a net/rpc handshake would replace a working transport to gain nothing -- and because the Port/Listen/Health fields on Service were designed for exactly this and had no runtime that read them. It reuses everything that matters (staged copy-then-verify, env allowlist, restart budgets, the permanent-vs-transient split, and the rule that a running process is not health) and adds only launch + probe. The doctor row is the point: a pack daemon was the one host-exec facet with NO row, so when snow-proxy died the warehouse capability degraded in total silence. Then the third launch form. UnitSpec described only two ways to start a unit -- self-exec or a SHA-pinned external path -- so Validate rejected the one thing a pack daemon actually is: a binary the user built on their own Mac, which no shared manifest can carry a SHA for. The pack was accepted, the tree accepted the daemon, and then the unit it was handed could not describe itself, so snow-proxy never started and the failure surfaced only in a serve log. UnitSpec.Command closes that, joins identity() so a reattach cannot confuse two daemons, and is refused alongside a sha pin -- a pin naming a file the spec never reads is worse than no pin, because it reads as a guarantee. The consent screen renders it UNPINNED so the absence is the user's call, not a silent property. And the startup budget. waitHealthy took HealthTimeout, the per-probe budget, for a daemon's FIRST health check -- so snow-proxy (~1.5s warm, ~3.5s cold) lost a coin flip against 3s, and a lost flip removes the unit from the tree, which a first-run user meets as a dead warehouse. Handshake already meant exactly 'how long may a unit take to become usable', so it now covers both unit kinds rather than growing a fourth timeout, and AddDaemon's outer wait derives from the same budget instead of firing while the inner poll is still legitimately running. The added lines are that comment plus countTrue; the launch path itself did not grow. Then hostpath.go, prod_loc 1703 -> 1815 and globals 29 -> 30 (errCommandNotFound). 'Resolved on PATH at launch' meant nothing in the environment that actually launches serve: launchd hands a login service PATH=/usr/bin:/bin:/usr/sbin:/sbin, and every host binary in this system lives elsewhere -- snow-proxy and slack-mcp in ~/.local/bin, snow and gog and op under Homebrew. So the daemon resolved perfectly in a developer's shell and failed PERMANENTLY under the managed service, which is the only way real users run it, while the error said 'is not on PATH' to someone whose interactive PATH did have it. The same gap hit the child: a daemon inheriting launchd's PATH starts, binds, and answers its health check while being unable to find the vendor CLI it shells out to -- every row green, capability broken, which is the defect class this whole release was about. Resolution now searches PATH then three code-pinned directories, the error names every one of them, and a unit that allowed PATH gets the same list so it can find its own tools. The list is pinned in code, not config: a manifest that could extend it could point `command` anywhere while keeping the same name on the consent screen. Then 1815 -> 1825: a daemon never recorded its probe latency, so `pix serve status` printed probe=0us for an HTTP GET over a socket. Zero reads as 'instant', not as 'never measured', and the latency of the probe that timed out is the number that explains the eviction after it -- which is why the go-plugin unit has always timed every probe, failures included. Now both kinds do. Then 1825 -> 1919: a port probe cannot tell 'my child is serving' from 'someone else owns this port', and that gap ran snow-proxy 4,057 times over six hours on Mark's Mac. An orphan held :11442 (pkill of a foreground serve leaves the daemon behind, because Setpgid puts it in its own group), so every generation was: child starts, cannot bind, exits 1, while waitHealthy dialled the port, got the ORPHAN's 200 and reported healthy. `pix doctor` said `daemons OK 1 answering` throughout, because something genuinely was. The fix is a PRE-FLIGHT dial before launching: nothing of ours is running yet, so anything listening is someone else's and the bind is guaranteed to fail. It is deterministic where catching it afterwards is a race, because a child that exits in microseconds may not be reaped before the probe returns -- which is why the obvious after-the-fact check failed its own test first. It waits up to the stop budget before concluding, so a restart that is moments from releasing its own socket is not condemned. waitHealthy also watches the child now and refuses to believe a passing probe once it has exited, which covers the non-port causes. Then prod_loc 1919 -> 1971: tryReattach REAPS the live child it refuses to adopt. Refusing was already right; walking away was the bug. When the persisted state names this unit and a pid proven alive and ours, but the identity fingerprint or the negotiated protocol version no longer matches, that pid is our OWN earlier generation -- the ordinary upgrade (pix-host rebuilt or upgraded while a hard-killed supervisor's child kept running). The old code dropped the record and left the process running: unsupervised, now unfindable, still holding memory's exclusive store flock, so every later spawn died on the lock and `serve` could not start again at all. Observed on a real host on 2026-08-23 (`supervise: memory reattach_rejected executable identity changed`, then a launchd KeepAlive respawn loop every 10s against a plugin orphaned since Aug 15), and reproduced both ways against the real binary before and after. The switch reorders so liveness/address precede the two supersede cases, which is what makes their kill decision sound; the kill itself is unchanged policy, factored into reapVerifiedOrphan -- now killVerifiedOrphan's ONLY caller, so both the dispense-failure path and the new supersede path reach an OS process through exactly one set of proofs (revalidateOrphan, start-time-bound against pid reuse). stopChild stops revoking the reattach record when its kill missed the stop budget and the child is not confirmed exited: that was the same orphan-with-no-record leak by a second route. Three `pid <= 0` guards (killVerifiedOrphan, revalidateOrphan, processAlive) are unreachable today but sit on primitives where a non-positive pid means the process GROUP -- the difference between reaping an orphan and killing the supervisor and everything it started. Comments were compressed hard first (+80 -> +52); no new exports or globals. A deliberate, reviewed raise of a shrink-only budget."
    },
    "pix/host/sys": {
      "prod_loc": 572,
      "exports": 23,
      "globals": 16,
      "edges": 1,
      "exits": 0,
      "_prod_loc_note": "prod_loc 489 -> 536, globals 12 -> 14: the cross-process flock is BOUNDED. It was a plain blocking LOCK_EX, and a blocking wait with no deadline is a hang with no diagnosis — `pix pack use` registered every server and then went silent, waiting inside its post-commit wrapper refresh for a lock another process held. A post-commit side effect is exactly the code that is allowed to fail with a note rather than take the terminal with it, and it could not: the caller can report a timeout, it cannot report a wait that never ends. acquireFlock polls LOCK_NB to a deadline (blocking on a goroutine plus a select would leak that goroutine, still holding the fd, for as long as the holder lives) and the error names the lock file so `lsof` finds the holder. Ordinary contention still acquires. A deliberate, reviewed raise of a shrink-only budget. Then 536 -> 554, globals 14 -> 16: the wait SAYS SO. Bounding it fixed the hang and left the symptom that made the hang hard to read — a blank terminal, indistinguishable from a wedged one. A real `pix setup` sat silent for ten seconds while working perfectly, and there was no way to tell that from the outside. Past a second of waiting, acquireFlock names the lock on stderr, once; an uncontended lock stays silent, which is the second test. A deliberate, reviewed raise of a shrink-only budget. Then 554 -> 563, exports 22 -> 23: the notice moved to a PARAMETER (LockNotifying) after the architecture tests refused the first cut twice over — a function-valued package var is a dependency seam and an L3/L4 technique, not a leaf's (wiring_test), and printing put a terminal stream in a package whose only sanctioned stream use is handing a terminal to a child process (processboundary_test). Both rules point the same way: the layer holding a writer owns the message. A deliberate, reviewed raise of a shrink-only budget. Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise."
    },
    "pix/host/sys/systest": {
      "prod_loc": 322,
      "exports": 6,
      "globals": 1,
      "edges": 1,
      "exits": 0
    },
    "pix/host/uat": {
      "prod_loc": 1534,
      "exports": 42,
      "globals": 11,
      "edges": 1,
      "exits": 0,
      "_prod_loc_note": " Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Rebase integration:  Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Then prod_loc 866 -> 967, exports 23 -> 24: ResolveAttachRegistration makes dev intent enforceable on attach, requiring both the create-time record and current gateway registration; fresh registration is verified through sbx mcp ls before sandbox creation and rolled back by its exact UAT-owned name on ambiguity. Registration identity is path-safe and exact, and teardown now removes the validated session state directory instead of leaking one per dev sandbox. A deliberate, cross-vendor-reviewed raise. LegalVocabulary is the one exported source of truth for the scenario validator and capability response, replacing a second action/assertion registry that could drift; the same refactor shrinks prod_loc 967 -> 955. Exports 24 -> 25, a deliberate, cross-vendor-reviewed raise of a shrink-only budget. Then the U1 session-worker transport core (docs/design/self-development-uat.md): SessionSocketPath/ValidateSocketDir/ValidateSocketPath/ListenSocket/DialSocket give the session-scoped UAT relay socket its path derivation, symlink/permission/ownership hardening, and bounded-retry connect, all built on the same session-id/root rules RegisterMCP/removeSessionState already enforce. prod_loc 955 -> 1160, exports 25 -> 31 (the five new socket functions plus SocketFileName), globals 3 -> 5 (SocketFileName and the unsupported-platform sentinel error). A deliberate, reviewed raise of a shrink-only budget. Then U2 of the self-development UAT auth repair (docs/design/self-development-uat.md): worker.go wires `pix run --dev` to actually start and supervise `pix-host uat-worker` from the launcher's own process. EnsureWorker dials the session socket first so two racing attachers adopt a live worker rather than unlinking or replacing it, and only starts (or replaces a confirmed-stale) worker when nothing answers, killing and reaping it on a bounded readiness timeout rather than ever claiming dev UAT while the relay is dead. StopWorker is the one place that ever signals a recorded worker pid, gated on a ps-verified session/pid identity match, the same standard service/ctl.go's verifyServeProc applies to `pix-host serve`; DeleteRegistration now calls it before removeSessionState so a hard-crash/orphan sweep terminates a stale worker before wiping its socket and pid record out from under it. worker_windows.go keeps the package building on Windows (StopWorker is a no-op there, matching socket_windows.go's precedent) since lifecycle.go calls it unconditionally. prod_loc 1160 -> 1534, exports 31 -> 42 (WorkerRecord + its fields, ReadWorkerRecord, EnsureWorkerDeps + its fields, DefaultEnsureWorkerDeps, WorkerSpawn, WorkerArgv, EnsureWorker, StopWorker, WorkerReadyAttempts/WorkerReadyDelay, WorkerPIDFileName), globals 5 -> 11 (the exported constants above plus the unexported timeout/poll-interval consts). A deliberate, reviewed raise of a shrink-only budget."
    },
    "pix/host/uatenvmatrix": {
      "_prod_loc_note": "Initial budget, seeded alongside the arch-metrics fix that moved candidate_smoke's env-matrix override seam (RunForCandidateSmoke) out of workflow/uat and into this L1 package: uatenvmatrix.CheckNames/Run/RunForCandidateSmoke, the named environment checks (E0.1-E0.6), and their own override-detection glue. Deliberate reviewed raise: prod_loc 1145 -> 1156 fixing the proven UAT bootstrap bug (run run-20260823-155824-4d96352e, environment_create_then_exec_invocation): isolatedExecEnv rehomed HOME under phaseDir, hiding the host's sbx/Docker Desktop authentication, so every check shelling out to sbx failed with 'Not authenticated to Docker'. HOME/DOCKER_HOST/DOCKER_CONFIG now pass through unmodified (mirroring workflow/uat/execute.go's candidate_smoke override) while every Pix XDG root and PIX_CONFIG still isolate under phaseDir; the growth is documentation of that seam plus the added passthrough keys, no new isolation surface. Deliberate reviewed raise: prod_loc 1156 -> 1287, globals 15 -> 16 (U3 of the self-development UAT auth repair, docs/design/self-development-uat.md): the four checks that create a real pix-uatenv-* fixture (create_then_exec, uses_local_candidate_image, recreate_boundary, custom_agent_ollama) previously never tore their fixture down, leaking a real sandbox on every successful host UAT run. cleanup.go adds the one shared, receipt-gated cleanupCreatedFixture helper (docs/design/environments.md section 9.3's `sbx env rm -f` policy, scoped further to the uatenvFixturePrefix const — the added global — so it can never touch anything outside this matrix's own pix-uatenv-* namespace) plus a fresh-probe/removal call wired via `defer` into each of the four checks; fixtures.go/check_local_image.go/check_failed_create_cleanup.go also gained an explicit `name:` field in every literal YAML body, since a real `sbx env create` has no other way to learn the name a fixture's own receipt assertion expects to see. No new capability surface, no widened scope: the growth is the teardown this package always owed the host it runs against. Deliberate reviewed raise: prod_loc 1287 -> 1379, globals 16 -> 17 (host UAT run run-20260823-200503-777c37e1, `sbx env create` failing with `resolve kits: kit reference \"./kit\": path does not exist`): customAgentFixture and ollamaCapabilityFixture both author `kits: - ./kit` but nothing ever materialized that directory on disk. fixture_write.go adds one shared writeAuthoredFixture helper (routes both checks' fixture writes through it, so the fix cannot silently land for one named check while a sibling keeps the identical bug) plus a literal, package-owned minimal kit-spec v2 body (fixtureKitSpecYAML) and its one pinned image constant (fixtureKitSpecImage, the added global) for the materialized kit. No new capability surface: this is authoring the missing fixture bytes a real `sbx env create` already expected to find. Deliberate reviewed raise: prod_loc 1379 -> 1473, exports 6 -> 8, globals 17 -> 18 (the candidate-owned environment-matrix seam, docs/design/self-development-uat.md; host run run-20260823-201941-8f7b648b proved the worker still executed its own statically linked pre-candidate uatenvmatrix rather than the SUBMITTED candidate's own uat-env-matrix binary). RunForCandidateSmoke/candidateOverride are DELETED: candidate_smoke no longer calls this package's Run directly at all -- it execs the candidate's own `pix-host uat-env-matrix` child process instead (workflow/uat/env_matrix.go), through a mock seam now owned at that L3 Runner boundary. In their place, cli.go adds the new, intentionally-not-publicly-documented `pix-host uat-env-matrix` entry point: ParseArgs validates argv (required absolute existing --out-dir/--steps-dir, a fully qualified --image-tag inside the reserved docker.io/mcavage/pix:uat- namespace, no positional arguments) and RunCLI dispatches to a pure `--list-checks` reporting mode (CheckNames, no host tools) or to Run. The two new exports are CLIArgs and ParseArgs/RunCLI's exported surface; the one new global is uatImageNamespacePrefix. Deliberate reviewed raise: prod_loc 1473 -> 2814, globals 18 -> 46 (Story 0 closeout, docs/upstream/sbx-0.39-environments.md). The six typed host-backed checks (E0.1 create_then_exec, E0.2 uses_local_candidate_image, E0.3 recreate_boundary, E0.4 failed_create_cleanup, E0.5 rm_scope_refusal, E0.6 custom_agent_ollama) plus E0.7's interpolation-observation phase land bounded per-check evidence, the shared receipt-gated cleanupCreatedFixture helper deferred into every fixture-creating check, authored custom-agent kit fixtures, exact-tag candidate-image evidence with no unobservable digest claim, the `sbx --version` -> `sbx version` fallback, and the two-fixture/three-form interpolation observation -- all of it duplicated rather than imported from this package's L1 sibling pix/host/uatmatrix, per matrix.go's own no-sibling-L1-import rule. This is the real cost of six receipt-gated host checks proven against a real sbx binary, not capability creep. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 2814,
      "exports": 8,
      "globals": 46,
      "edges": 0,
      "exits": 0
    },
    "pix/host/uatmatrix": {
      "_prod_loc_note": "Initial budget: isolated host-backed memory candidate checks. The package runs committed pix/pix-host binaries against run-local config, state, ports, SQLite fixtures and a deterministic fake Ollama; it covers cold/warm recall, stale-daemon wording, explicit and experimental-auto capture, the daily budget, source spoofing, migration rollback, forget errors and supervised plugin restart without touching the normal daemon or store. Process cleanup verifies candidate identity and signals the entire run-local process group, including the orphan-only case where the serve parent exited first, so a wedged plugin cannot survive the run. Then prod_loc 1495 -> 1511, exports 2 -> 3: CheckNames exposes the exact matrix registry to capability and dry-run responses, so advertised memory coverage cannot diverge from execution. A deliberate, cross-vendor-reviewed raise of a shrink-only budget. prod_loc 1511 -> 1520: waitRPCReady now requires ready=true instead of treating \"identity answered without an RPC error\" as up. Those were equivalent only because the front door could not answer `identity` until its unit was dispensed; now that a bound listener answers immediately with ready=false so probes never hang, the old form returned on its FIRST poll and raced the real work behind it -- which is how TestRealMemoryMatrix_FullSuite failed on main with \"remember before kill: pix-memory is starting\". A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 1520,
      "exports": 3,
      "globals": 3,
      "edges": 0,
      "exits": 0
    },
    "pix/host/unitreport": {
      "prod_loc": 137,
      "exports": 6,
      "globals": 2,
      "edges": 0,
      "exits": 0
    },
    "pix/host/unitreport/unitreporttest": {
      "prod_loc": 117,
      "exports": 3,
      "globals": 1,
      "edges": 1,
      "exits": 0
    },
    "pix/host/workflow/doctor": {
      "_prod_loc_note": "raised together for ONE change: `pix doctor` and `pix setup` demanded an anthropic/openai/google key from hosts whose models need none. A pack's sbx-session backends carry their own credential (a Docker login session the sbx proxy injects inside the sandbox, unreplayable from the host by design), so the key store lists nothing and the required row went red prescribing `pix models add anthropic` -- a key nothing would read. `pix run`'s launch gate already skipped the key check on exactly this host (inference.ConfiguredKeylessInference); ProviderKeyProbe.Keyless + inference.KeylessBackends give the same carve-out to the two verbs that lacked it, so all three stop disagreeing about one host. Distinct from Callable, which answers \"this key routes nowhere\"; Keyless answers \"no key was ever the credential\". provision's new edge is importing inference for that one predicate rather than re-deriving a second opinion. A deliberate, reviewed raise of a shrink-only budget. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then the adversarial-verification round: a probe was wrapped in op run based on the SERVER's credentials rather than the probe's own needs, so BambooHR's credential-free 'docker image inspect' inherited a 1Password dependency it never had — turning a millisecond answer into 'not checkable' on a locked vault, and making a read-only diagnostic provoke an authorization prompt. It is wrapped now only when the probe invokes the server's own binary. And 'pix pack use' swallowed a registration failure into a note and exited 0, so adopting a pack on a machine missing one of its commands reported success for servers it had not registered. Then the daemon runtime: a third [[services]] runtime for a long-running HOST process that already speaks its own protocol on a loopback port. It exists because go-plugin is the wrong shape for one -- snow-proxy is reached over HTTP by an in-sandbox wrapper, so a net/rpc handshake would replace a working transport to gain nothing -- and because the Port/Listen/Health fields on Service were designed for exactly this and had no runtime that read them. It reuses everything that matters (staged copy-then-verify, env allowlist, restart budgets, the permanent-vs-transient split, and the rule that a running process is not health) and adds only launch + probe. The doctor row is the point: a pack daemon was the one host-exec facet with NO row, so when snow-proxy died the warehouse capability degraded in total silence. Then 723 -> 742: the github row joins the probe set, plus an Options.GitHubScope seam. The seam is not optional decoration: without it the doctor tests would ask the real sbx and assert against whatever secret the developer's own machine happens to hold, which is a test that passes or fails by accident of environment. Then E1.1 (docs/design/environments.md section 5.6, AC-20): RunDoctor now consults health.SbxVersionGate against the sbx row it already computed and, when blocked, prints the exact PRD section 5.6 copy (health.SbxVersionGateMessage) and forces a non-zero exit — including when the underlying SbxProbe result stayed Unknown (an unparsable version), since the base SbxProbe classification for that case is deliberately unchanged and the exit code is now derived from the gate rather than snap.ExitCode() alone. --json is unaffected: it still fails closed on the exit code but never mixes the prose line into the JSON stream. prod_loc 742 -> 756. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 756,
      "exports": 23,
      "globals": 10,
      "edges": 12,
      "exits": 0
    },
    "pix/host/workflow/launch": {
      "_prod_loc_note": "2458 -> 2503, exports 109 -> 112: personal context (skills + standing AGENTS.md) is now mounted unconditionally at the context ROOT, so a session can write its FIRST skill from inside the sandbox and keep the whole dir in git. Previously the mount was gated on the dir already having entries, which made the first skill impossible to author in-sandbox, and only skills/ was mounted, so AGENTS.md was invisible. New exports: MountDirs (the mount set, now distinct from the skill set), PersonalSkillsDir (one spelling shared by loader and mount), EnsurePersonalContextDir. A deliberate, reviewed raise of a shrink-only budget. Then 2503 -> 2543, globals 42 -> 43: teardown now translates pi's parting \"To resume this session: pi --session-dir ...\" into the host command that actually works (`pix run -- -c`), and only when a session file provably exists. Suppressing pi's line would have been wrong: .pi-sessions is in the workspace mount, so the session genuinely outlives the sandbox; only the dialect was unusable. Then 2543 -> 2560, globals 43 -> 45: `pix ls` grows a HELD BY column. A held box is one a shell is still attached to and teardown correctly refuses to remove; an unheld one is what `pix rm --orphans` sweeps. Unreadable lease state renders '?', never '—', because the free value is a PROMISE that teardown will remove the box. A deliberate, reviewed raise of a shrink-only budget. Then 2560 -> 2564: the column says 'session' rather than 'shell', with the comment explaining why — the holder is a `pix` process on the HOST, and a shell exec'd inside the sandbox holds no lease at all. The first reader of the column asked which one it meant. Then 2564 -> 2582, globals 45 -> 47: the teardown journal guard got JITTER and headroom. A non-blocking poll has no queue, so 40 concurrent appenders retried on the same 5ms boundary, collided, and the unluckiest starved past a 2s budget — green on every laptop, red on a loaded CI runner. Jitter is the fix; the longer budget is only headroom. A deliberate, reviewed raise of a shrink-only budget. Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Rebase integration: 2458 -> 2503, exports 109 -> 112: personal context (skills + standing AGENTS.md) is now mounted unconditionally at the context ROOT, so a session can write its FIRST skill from inside the sandbox and keep the whole dir in git. Previously the mount was gated on the dir already having entries, which made the first skill impossible to author in-sandbox, and only skills/ was mounted, so AGENTS.md was invisible. New exports: MountDirs (the mount set, now distinct from the skill set), PersonalSkillsDir (one spelling shared by loader and mount), EnsurePersonalContextDir. A deliberate, reviewed raise of a shrink-only budget. Then 2503 -> 2543, globals 42 -> 43: teardown now translates pi's parting \"To resume this session: pi --session-dir ...\" into the host command that actually works (`pix run -- -c`), and only when a session file provably exists. Suppressing pi's line would have been wrong: .pi-sessions is in the workspace mount, so the session genuinely outlives the sandbox; only the dialect was unusable. Then 2543 -> 2560, globals 43 -> 45: `pix ls` grows a HELD BY column. A held box is one a shell is still attached to and teardown correctly refuses to remove; an unheld one is what `pix rm --orphans` sweeps. Unreadable lease state renders '?', never '—', because the free value is a PROMISE that teardown will remove the box. A deliberate, reviewed raise of a shrink-only budget. Then 2560 -> 2564: the column says 'session' rather than 'shell', with the comment explaining why — the holder is a `pix` process on the HOST, and a shell exec'd inside the sandbox holds no lease at all. The first reader of the column asked which one it meant. Then 2564 -> 2582, globals 45 -> 47: the teardown journal guard got JITTER and headroom. A non-blocking poll has no queue, so 40 concurrent appenders retried on the same 5ms boundary, collided, and the unluckiest starved past a 2s budget — green on every laptop, red on a loaded CI runner. Jitter is the fix; the longer budget is only headroom. A deliberate, reviewed raise of a shrink-only budget. Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Then WriteMemoryCaptureFile (launch/run.go), the launch-scoped .pix/memory-capture marker writer the sandbox capture extension reads once at load -- same shape as WriteOllamaBridgeFile, one new exported function. prod_loc 2614 -> 2634, exports 112 -> 113. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 2628,
      "exports": 113,
      "globals": 47,
      "edges": 15,
      "exits": 0
    },
    "pix/host/workflow/models": {
      "_prod_loc_note": "1084 -> 1091: `models add` no longer tells the user to run `pix models route`. That was busywork presented as a required step: every `pix run` already recompiles the intent map from current bindings. The line now states that routing updates itself. A deliberate, reviewed raise of a shrink-only budget. 1091 -> 1369: pix silently dropped every Ollama model the user pulled that the shipped catalog did not know, because ConfigureOllamaInference only bound tags intersecting the registry. Binding them requires deciding, per tag, whether it is LOCAL (free, private, charged against this machine's RAM) or Ollama CLOUD (remote, metered, prompt content leaves the laptop). That is a privacy boundary, so it is a three-signal cascade that refuses rather than guesses, plus a real RAM gate for unknown local tags, plus an ingestion-boundary name grammar so a rogue listener on the Ollama port cannot forge or erase setup output with ANSI escapes. A deliberate, reviewed raise.",
      "prod_loc": 1369,
      "exports": 19,
      "_globals_note": "6 -> 10: the untrusted-name pattern and its length cap, the response-body size cap, and the shrinkable /api/tags timeout seam. The timeout is a var rather than a const specifically so a hermetic test can shrink it; before this change nothing could, because this package had no test files at all.",
      "globals": 10,
      "edges": 6,
      "exits": 0
    },
    "pix/host/workflow/pack": {
      "_prod_loc_note": "prod_loc 3049 -> 3062, globals 4 -> 5: ONE seam, paying for itself many times over. propagateConfig's real implementation runs `launchctl kickstart -k` against the developer's own pix LaunchAgent, and every test here that adopts a pack went through it -- so a test run restarted the live daemon on the machine running it, serially, blocking on each. That was 507s of a 578s `go test ./...` locally against a fast gate whose own documented go-test baseline is 32.8s; CI never saw it because there is no agent there to kick. Behind the var, this package is 2.8s and the whole gate is 47s instead of 9.5 minutes. The new global IS the seam. A deliberate, reviewed raise. Then prod_loc 3062 -> 3166: the declarative setup runner (checkRequires/applySteps) plus rendering require/apply on the Tier-1 consent screen. The screen is not optional detail \u2014 a declarative step still names binaries and argv that run on this Mac, so consenting means seeing them, and summarising them as a count would be the same failure as the registration that read \"ready\" for a command that did not exist. Then 3166 -> 3187: the Tier-1 consent screen printed `op run -- <cmd>` for EVERY host command, including one that declares no credentials and is therefore never wrapped. A consent screen whose whole job is to show you the real command must not show a different one, so the wrapper is now rendered only when the server declares credentials, and the credential NAMES it receives are listed the way a container's already were. Then 3187 -> 3207: a pack's health `probe` is EXECUTED on the host by `pix doctor`, but was neither shown on the Tier-1 consent screen nor covered by the fingerprint \u2014 so a pack could change a command pix runs on your machine without re-gating, and you would never have seen the original. It is executable intent and is now treated as such on all four transports. Found by an independent reviewer, not by the author. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered \u2014 and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `\u2713 ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then the review round: `pix secret check` was UNBOUNDED, so a locked 1Password vault (the normal state of a fresh laptop) hung it forever after printing FAIL for references that were perfectly correct, sending users to re-check something that was fine. It is bounded now, with 'could not check' as a third answer, and the stale-key line offers removal instead of advice that leads nowhere. Then the review round: `pix secret check` was UNBOUNDED, so a locked 1Password vault (the normal state of a fresh laptop) hung it forever after printing FAIL for references that were perfectly correct, sending users to re-check something that was fine. It is bounded now, with 'could not check' as a third answer, and the stale-key line offers removal instead of advice that leads nowhere. Then the adversarial-verification round: a probe was wrapped in op run based on the SERVER's credentials rather than the probe's own needs, so BambooHR's credential-free 'docker image inspect' inherited a 1Password dependency it never had \u2014 turning a millisecond answer into 'not checkable' on a locked vault, and making a read-only diagnostic provoke an authorization prompt. It is wrapped now only when the probe invokes the server's own binary. And 'pix pack use' swallowed a registration failure into a note and exited 0, so adopting a pack on a machine missing one of its commands reported success for servers it had not registered. Then the first REAL pix setup run, which exposed what four review rounds could not because no agent was allowed to run that verb: the credential solicitor skipped every integration with a setup hook -- which is every integration that needs a credential -- so nothing was ever asked for and setup died on the first missing reference having offered no way to supply it. Credentials are now solicited BEFORE registration too, because registering first produced a bare, unwrapped server on a clean machine: exactly the broken state this whole surface exists to prevent. Requirements report all at once with pack-authored guidance, and a failed setup phase can no longer be headlined ready. Then the daemon runtime: a third [[services]] runtime for a long-running HOST process that already speaks its own protocol on a loopback port. It exists because go-plugin is the wrong shape for one -- snow-proxy is reached over HTTP by an in-sandbox wrapper, so a net/rpc handshake would replace a working transport to gain nothing -- and because the Port/Listen/Health fields on Service were designed for exactly this and had no runtime that read them. It reuses everything that matters (staged copy-then-verify, env allowlist, restart budgets, the permanent-vs-transient split, and the rule that a running process is not health) and adds only launch + probe. The doctor row is the point: a pack daemon was the one host-exec facet with NO row, so when snow-proxy died the warehouse capability degraded in total silence. Then 3363 -> 3408: setup's pack adopter passed interactive=false as a hardcoded constant, so a step whose remediation needs a terminal was refused on a run that had just prompted the user for a y/N and two 1Password references -- and the refusal told them to re-run without --yes/--non-interactive, flags they had never passed. The cost fell entirely on NEW users, which is why four review rounds missed it: a new user has no Slack grant by definition, so that step could never pass, its fix could never run, and `pix setup --pack` could never complete. Interactivity is measured now (setupInteractivity: !assumeYes && a real TTY, both load-bearing -- TTY alone would let --yes open a browser, --yes alone would prompt into a pipe), extracted into a function precisely because nothing could observe a constant. Also reasonBlock: a check's reason is a multi-line report carrying the pack's own guidance, and interpolated into a parenthetical it produced an eight-line sentence with the closing paren stranded at the end. It is a block under its heading, and absent entirely for an executable hook, which reports no reason at all. Then 3408 -> 3849, trustsummary.go: the consent screen was about seventy lines for a real pack, and Mark read one as a new user -- 'my god this is an overwhelming wall of text'. That is not cosmetic: a consent screen nobody reads is not consent, so length was costing the gate the thing it exists for. Most of it was REPETITION -- slack appeared once as a Host MCP with its health check and again as an Ensures: block with the same check plus a guidance paragraph -- and per-integration grouping scattered the one question a user has, what will execute on my machine, across the whole screen. The summary inverts it: counts, then ONE flat list of every command that runs here, which answers the security question better than the grouped view did. The detailed screen is unchanged and one keypress away (`d` at the prompt, or --details for a non-TTY review). The governing rule is tested: every fact the FINGERPRINT covers stays visible by DEFAULT, because a fact that re-gates while being invisible on the screen the user is re-asked on turns re-consent into a mystery prompt. That rule caught two of my own omissions mid-change -- remote URLs (host-only would have hidden a repointed path) and the env names that are not solicited credentials (GOG_ACCOUNT, literal EnvValues), both fingerprinted and both briefly dropped. Pack-authored HINTS are the one thing moved behind --details: prose, and prose cannot change what executes. Then 3849 -> 3912: the pack's guidance for a credential now prints AT the prompt that asks for it. Mark, after a successful run: 'i got asked for gog keyring password, but like how would a new user even get to that point as a prereq?' They would not have. The pack authors that guidance -- invent a passphrase, make a 1Password item, copy its secret reference -- and it rendered only on the adoption screen dozens of lines above the prompt, then behind the detail key once the screen was summarised. The text existed; the user never met it where it was actionable. Matched by env name, which the integration and the requirement already share, so a pack gets this by authoring one hint. Also: solicitPackCredentials RETURNED SILENTLY when the 1Password CLI was absent -- no prompt, no reason -- and the user then met a setup failure naming `pix secret set`, which also needs op. Every message pointed at a tool nothing had mentioned. It now says which references are needed and what to do, and deliberately does not prompt for a value it has nowhere to put. Then 3912 -> 3936: a satisfied setup step now prints the check that proved it. Mark, after a reset and reinstall: 'I still don't understand if gog gets set up or not as i'm never asked. i only get asked for keyring 1pass reference?' Nothing was wrong -- gog's keyring lives outside the three directories reset moves aside, so it was still authorized, the probe passed, and the interactive authorization correctly did not run. But `\u2713 <label>: ready` cannot be told apart from 'skipped', so the right outcome was indistinguishable from the step being passed over. Only what PROVES a step is printed: a declarative step's probe requirements, or an executable hook's check argv. `bin` and `op-ref` show that something exists rather than works, so their passing is not evidence. The hook is included so Snowflake does not read as less verified than its neighbours when it is equally verified. Then 3936 -> 3988: setup's `probe` requirement now runs through the SAME op-run wrapper the gateway spawns the server with. Run raw it answered a question nobody asked -- it proved the binary works in a shell that already has what it needs. Mark moved his 1Password items to another vault, the references went stale, the gateway could not start google-workspace at all, and setup printed 'ready, verified by: gog --readonly gmail labels list' because gog's keyring was already unlocked. doctor, which wraps, called it broken in the same minute. mcp.OpRunWrap's own comment already stated the rule and named two callers; this was the third. Only a step whose integration DECLARES credentials is wrapped, matching mcp.ExecArgv exactly and for its reason: `op run --env-file` resolves EVERY reference in the file, so wrapping a credential-free probe would make it share fate with unrelated entries -- and a stale BambooHR reference stopping gog is precisely how this host broke. Then 3988 -> 4061: `pix setup` can no longer be deadlocked by the pack it is setting up. Registration is adoption's last post-commit step, so a pack whose MCP server is a host command it also knows how to INSTALL failed adoption on every clean machine \u2014 and the failure returned before the setup hooks ran, so the fix the error named (\"the pack's setup step does this\") was the one thing that could not happen. Two people hit it on gm-pix-pack, on two different integrations, and neither could get past it without hand-installing a binary. mcpRegisterError types that one post-commit failure so adoptForSetup can tell it apart from a refusal that adopted NOTHING (a declined gate, a bad pin) \u2014 those stay fatal exactly where they were \u2014 and registerActivePackMCP asks again after the hooks have run, making the same call adoption makes. A failure that SURVIVES setup is still non-zero, and a deferral with nothing to retry reports the original failure rather than an empty loop's success: deferring is not forgiving, it is ordered. A deliberate, reviewed raise of a shrink-only budget. Then 4061 -> 4066: the retry runs even when a setup step FAILED. A pack's steps are independent, so an unrelated step dying (a broken OAuth scope, an expired grant) says nothing about the command an earlier step just installed \u2014 and returning first left that command's server unregistered until someone ran `pix mcp add` by hand, which is a run that half-works and has to be done twice. Both failures are reported; neither hides the other. Then 4071 -> 4091: pack owns the lock-wait message, since pack is the layer with a writer. withPackTrustLockOn narrates the two waits a user actually sits through \u2014 the post-commit wrapper refresh, and the commit itself, which is where `pix setup` went quiet right before `active pack ->` and got Ctrl-C'd for looking hung. A deliberate, reviewed raise of a shrink-only budget. Then E1.4, a PURE structural extraction: canonical identity, the fingerprint engine, the acceptance-record shape, the flock-serialized fresh-load->mutate->save shape, atomic document I/O, symlink refusal, and content hashing moved to the new L1 pix/host/hosttrust, leaving trust.go/truststore.go as thin callers over hosttrust's primitives. prod_loc 4091 -> 4017, exports 28 -> 26, globals 5 -> 4, edges 11 -> 10 (one net edge added to hosttrust, several stdlib-only imports dropped). A shrink-only ratchet: no fingerprint, file path, JSON shape, or externally observable behavior changed (trust_fingerprint_compat_test.go and every pre-existing pack-trust test pass unmodified).",
      "prod_loc": 4017,
      "exports": 26,
      "globals": 4,
      "edges": 10,
      "exits": 0
    },
    "pix/host/workflow/provision": {
      "_prod_loc_note": "raised together for ONE change: `pix doctor` and `pix setup` demanded an anthropic/openai/google key from hosts whose models need none. A pack's sbx-session backends carry their own credential (a Docker login session the sbx proxy injects inside the sandbox, unreplayable from the host by design), so the key store lists nothing and the required row went red prescribing `pix models add anthropic` -- a key nothing would read. `pix run`'s launch gate already skipped the key check on exactly this host (inference.ConfiguredKeylessInference); ProviderKeyProbe.Keyless + inference.KeylessBackends give the same carve-out to the two verbs that lacked it, so all three stop disagreeing about one host. Distinct from Callable, which answers \"this key routes nowhere\"; Keyless answers \"no key was ever the credential\". provision's new edge is importing inference for that one predicate rather than re-deriving a second opinion. A deliberate, reviewed raise of a shrink-only budget. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then the first REAL pix setup run, which exposed what four review rounds could not because no agent was allowed to run that verb: the credential solicitor skipped every integration with a setup hook -- which is every integration that needs a credential -- so nothing was ever asked for and setup died on the first missing reference having offered no way to supply it. Credentials are now solicited BEFORE registration too, because registering first produced a bare, unwrapped server on a clean machine: exactly the broken state this whole surface exists to prevent. Requirements report all at once with pack-authored guidance, and a failed setup phase can no longer be headlined ready. Then 1321 -> 1346, Step.ProbeProvesSubset: the harness skipped any step whose first check passed, on the rule that a proven capability must not be touched. That rule assumed a probe verifies what its apply performs, and for `pack` it does not. PackProbe answers 'is a pack active'; the pack apply adopts the pack AND runs its required setup hooks. So on every host where the pack was already active -- which is every host after the first run -- `pix setup --pack X` skipped all of that pack's hooks and printed ready having checked none of its integrations. A user re-running setup to repair a broken integration got a no-op and a green screen, with nothing in the output to tell the difference. Measured on the real pack: four required steps, zero run before, four reporting ready after. The added lines are the field and the comment explaining when NOT to reach for it (prefer strengthening the probe; this is for the case where the probe cannot reasonably cover the apply, since covering it here would mean running every pack's host requirement checks inside every `pix doctor`). Then 1346 -> 1357: the report now names `pix doctor`. Setup grades only the capabilities it OWNS, which is correct -- it must not claim to have checked something it cannot install -- but the rows it leaves out (mcp, daemons, memory) are the ones that say whether the work functions. Setup can install a launchd agent, verify the agent is loaded, and be entirely right while a unit under it is dead. Before this, `✓ ready` was the last line a new user saw and nothing told them a fuller report existed. Then 1357 -> 1372: a pack failure was printed TWICE -- in full in the `failed` row, then again as the command's final error, since that wraps the same cause. Two copies of an eight-line paragraph read as two different problems. The row shows the first line with an ellipsis; the full block appears once, at the end, where the exit code points and where it is the last thing the user sees. The cause is still wrapped, so nothing is lost and errors.Is still works -- a test pins that the top-level error carries the pack's own refusal. Then the setup key interview: `pix setup` reported the REQUIRED `providers` gap and printed `pix models add anthropic` instead of running it, so a host with no pack could not reach a passing setup at all -- it stayed unnoticed because a managed-inference pack satisfies that row keylessly, so the gap never appeared on the hosts being exercised. The providers step now carries an Apply that delegates to that same interview through provision.Composition (provision still may not import the workflow that performs a step), gated on a terminal and not --yes. Two exports come with it: provision.ErrSkipped, so a declined prompt reports as the choice it was rather than a failed apply or a repair that never happened, and cli.AskLine, ConfirmYN's open-ended twin -- shared because it must read straight off the stream like ConfirmYN does, and a caller wrapping it in its own bufio.Reader would steal input from every later prompt. The rest is Options.Progress and health.RunWithProgress: both check rounds are concurrent under an 8s per-probe budget, so setup could sit silent for sixteen seconds, which reads as a hang. Deliberate, reviewed raises of a shrink-only budget. 1504 -> 1514 in the same change: the interview re-reads keylessness at APPLY time rather than trusting the first check, because every probe in a round runs before any apply -- so `setup --pack <managed-inference pack>` graded providers while the pack was still unadopted, and would otherwise have asked for a credential nothing would read by the time the question was put. And the root cause the interview uncovered: setup passed ProviderKeyProbe its own ANTHROPIC_API_KEY/OPENAI_API_KEY/GEMINI_API_KEY list, while `sbx secret ls` lists a secret under the name it was STORED as -- a provider name -- so the required providers row was permanently red on a host with all three keys wired, synced and answering live requests. It now passes secret.ModelProviders, the same argument doctor and the launch gate already use. Then 1515 -> 1520: the ollama models probe reads StatusOff, not StatusReady, when config names no local model — nothing was actually verified, so a green check was a false one. provision.Run treats an off result the same as an already-proven-ready one for a step without ProbeProvesSubset, so it is skipped silently rather than falling through to a \"no automatic fix\" Skip entry for a capability the user never asked for. A deliberate, reviewed raise of a shrink-only budget. Rebase integration: raised together for ONE change: `pix doctor` and `pix setup` demanded an anthropic/openai/google key from hosts whose models need none. A pack's sbx-session backends carry their own credential (a Docker login session the sbx proxy injects inside the sandbox, unreplayable from the host by design), so the key store lists nothing and the required row went red prescribing `pix models add anthropic` -- a key nothing would read. `pix run`'s launch gate already skipped the key check on exactly this host (inference.ConfiguredKeylessInference); ProviderKeyProbe.Keyless + inference.KeylessBackends give the same carve-out to the two verbs that lacked it, so all three stop disagreeing about one host. Distinct from Callable, which answers \"this key routes nowhere\"; Keyless answers \"no key was ever the credential\". provision's new edge is importing inference for that one predicate rather than re-deriving a second opinion. A deliberate, reviewed raise of a shrink-only budget. Then the independent-review round: three clean-slate reviewers attacked the first cut and found real gaps, all closed here. A pack's `probe` runs on the host and was in neither the fingerprint nor the consent screen, so a pack could change what pix executes without re-gating. `ActiveServerMCP` read only the LAST activated pack, so on a composed stack doctor accused half of it of being undeclared and recommended `sbx mcp rm` on servers it had just registered — and it read a pack that fails to load as a pack that declares nothing, which turned one manifest typo into that same advice for every server on the host. MCP names and probe argv[0] were the only pack identifiers not name-gated, while landing in commands users paste into a shell. `NonSecretEnvNames` excluded a secret per-integration rather than pack-wide, so one integration could allowlist another's credential into plaintext. Probes could outrun their budget with no WaitDelay, which matters precisely because they now run through `op run`. And doctor's own report said `✓ ready` over a row that had proved an integration unusable, undercounted the unknowns behind the gaps, and printed one repair when several were needed. Each fix is a few lines; the volume is that there were many, and every one was a way of claiming more than had been verified. Then the first REAL pix setup run, which exposed what four review rounds could not because no agent was allowed to run that verb: the credential solicitor skipped every integration with a setup hook -- which is every integration that needs a credential -- so nothing was ever asked for and setup died on the first missing reference having offered no way to supply it. Credentials are now solicited BEFORE registration too, because registering first produced a bare, unwrapped server on a clean machine: exactly the broken state this whole surface exists to prevent. Requirements report all at once with pack-authored guidance, and a failed setup phase can no longer be headlined ready. Then 1321 -> 1346, Step.ProbeProvesSubset: the harness skipped any step whose first check passed, on the rule that a proven capability must not be touched. That rule assumed a probe verifies what its apply performs, and for `pack` it does not. PackProbe answers 'is a pack active'; the pack apply adopts the pack AND runs its required setup hooks. So on every host where the pack was already active -- which is every host after the first run -- `pix setup --pack X` skipped all of that pack's hooks and printed ready having checked none of its integrations. A user re-running setup to repair a broken integration got a no-op and a green screen, with nothing in the output to tell the difference. Measured on the real pack: four required steps, zero run before, four reporting ready after. The added lines are the field and the comment explaining when NOT to reach for it (prefer strengthening the probe; this is for the case where the probe cannot reasonably cover the apply, since covering it here would mean running every pack's host requirement checks inside every `pix doctor`). Then 1346 -> 1357: the report now names `pix doctor`. Setup grades only the capabilities it OWNS, which is correct -- it must not claim to have checked something it cannot install -- but the rows it leaves out (mcp, daemons, memory) are the ones that say whether the work functions. Setup can install a launchd agent, verify the agent is loaded, and be entirely right while a unit under it is dead. Before this, `✓ ready` was the last line a new user saw and nothing told them a fuller report existed. Then 1357 -> 1372: a pack failure was printed TWICE -- in full in the `failed` row, then again as the command's final error, since that wraps the same cause. Two copies of an eight-line paragraph read as two different problems. The row shows the first line with an ellipsis; the full block appears once, at the end, where the exit code points and where it is the last thing the user sees. The cause is still wrapped, so nothing is lost and errors.Is still works -- a test pins that the top-level error carries the pack's own refusal. Then the setup key interview: `pix setup` reported the REQUIRED `providers` gap and printed `pix models add anthropic` instead of running it, so a host with no pack could not reach a passing setup at all -- it stayed unnoticed because a managed-inference pack satisfies that row keylessly, so the gap never appeared on the hosts being exercised. The providers step now carries an Apply that delegates to that same interview through provision.Composition (provision still may not import the workflow that performs a step), gated on a terminal and not --yes. Two exports come with it: provision.ErrSkipped, so a declined prompt reports as the choice it was rather than a failed apply or a repair that never happened, and cli.AskLine, ConfirmYN's open-ended twin -- shared because it must read straight off the stream like ConfirmYN does, and a caller wrapping it in its own bufio.Reader would steal input from every later prompt. The rest is Options.Progress and health.RunWithProgress: both check rounds are concurrent under an 8s per-probe budget, so setup could sit silent for sixteen seconds, which reads as a hang. Deliberate, reviewed raises of a shrink-only budget. 1504 -> 1514 in the same change: the interview re-reads keylessness at APPLY time rather than trusting the first check, because every probe in a round runs before any apply -- so `setup --pack <managed-inference pack>` graded providers while the pack was still unadopted, and would otherwise have asked for a credential nothing would read by the time the question was put. And the root cause the interview uncovered: setup passed ProviderKeyProbe its own ANTHROPIC_API_KEY/OPENAI_API_KEY/GEMINI_API_KEY list, while `sbx secret ls` lists a secret under the name it was STORED as -- a provider name -- so the required providers row was permanently red on a host with all three keys wired, synced and answering live requests. It now passes secret.ModelProviders, the same argument doctor and the launch gate already use. Then 1515 -> 1520: the ollama models probe reads StatusOff, not StatusReady, when config names no local model — nothing was actually verified, so a green check was a false one. provision.Run treats an off result the same as an already-proven-ready one for a step without ProbeProvesSubset, so it is skipped silently rather than falling through to a \"no automatic fix\" Skip entry for a capability the user never asked for. A deliberate, reviewed raise of a shrink-only budget. Then memory_capture's config get/set/unset/help wiring, mirroring memory_watcher_model/run_intent exactly: one ConfigValue case, one ApplyConfigChange case (validated against config.MemoryCaptureModes), one ConfigKeysHelp block. prod_loc 1520 -> 1536. A deliberate, reviewed raise of a shrink-only budget. Revision pass: the memory_capture set/unset summary and ConfigKeysHelp entry both now state the mode change applies to new sandboxes only. prod_loc 1536 -> 1538. A deliberate, reviewed raise of a shrink-only budget. Phase-10 memory closeout 1538 -> 1560, memoryCaptureSummary: `config set memory_capture` reported the same new-sandboxes-only line for BOTH modes, which is wrong in the direction that matters most -- turning capture OFF is a host-side refusal that binds already-running sandboxes too. Turning it ON also needs a reachable watcher model, which this command cannot verify, so it names the check instead of claiming success. A deliberate, reviewed raise. Then E1.5: environmentKeyRefusal plus ApplyConfigChange's new prefix check refuse `config set/unset environment` and every `environments.<name>` key outright (config.Config.Environment/.Environments are real schema fields now, so falling through to the generic \"unknown key\" error would hide that they exist), and ConfigKeysHelp gained one line pointing both at `pix env`. prod_loc 1560 -> 1592. A deliberate, reviewed raise of a shrink-only budget. Then E1.5 pre-merge fix: every refusal/help/comment guiding unregistration now names `pix env forget` (the real verb) instead of `pix env rm` (reserved as a future pointer error, never guidance), and environmentKeyRefusal grew a branch for the exact `environments` key. prod_loc 1592 -> 1602. A deliberate, reviewed raise of a shrink-only budget. Then E1.5 final review fix: environmentKeyRefusal inferred exactness from the TRIMMED suffix (`strings.TrimPrefix(key, \"environments.\")` then compared to \"environments\"), which collapses the real per-name key `environments.environments` (an environment literally named \"environments\") into the bare-key case and silently drops the specific env name from the guidance. Exactness is now decided by the key string itself (`key == \"environments\"`), never by re-deriving it from a trim. prod_loc 1602 -> 1605. A deliberate, reviewed raise of a shrink-only budget.",
      "prod_loc": 1605,
      "exports": 31,
      "globals": 11,
      "edges": 12,
      "exits": 0
    },
    "pix/host/workflow/reset": {
      "prod_loc": 805,
      "exports": 10,
      "globals": 6,
      "edges": 4,
      "exits": 0,
      "_prod_loc_note": " Then reset's post-stop settle: a launchd boot-out returns when the unit is unloaded, not when the process has exited, and this daemon closes a sqlite writer and reaps a supervised child on the way out. Two seconds was not enough on a real machine, so reset refused to move the data directory and left a HALF reset (config moved aside, data and state kept, every MCP registration removed) for a pid that was gone moments later."
    },
    "pix/host/workflow/task": {
      "prod_loc": 833,
      "exports": 42,
      "globals": 10,
      "edges": 0,
      "exits": 0
    },
    "pix/host/workflow/uat": {
      "prod_loc": 3355,
      "exports": 61,
      "globals": 21,
      "edges": 10,
      "exits": 0,
      "_prod_loc_note": " Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Ship review: compute temporary browser profiles from the same global state root that creates them, so per-session lease cleanup removes the actual directory. 2878 -> 2902: realMCP.Auth diagnosed a closed browser-capture window two different ways for the same hang. The deadline case and the process-wait case become ready at the same instant -- cmdCtx derives from the same ctx, so an expired window tears the process down and whatever lands on waitErrCh next is that teardown -- and Go picks between ready select cases at random, so the run that passed on the PR failed on the push to main. The deadline now wins once it has passed -- checked on the PARENT ctx as well as captureCtx, since the latter is a child and cancellation reaches a child asynchronously, so the child's Err() can still be nil at the instant the parent releases the wait (a window that widens under -race and failed CI a second time when only the child was checked) -- and both routes share one errBrowserCaptureTimeout() so they cannot drift apart again. A deliberate, reviewed raise of a shrink-only budget. 2910 -> 2934: NewEventLog dropped NewEventStore's error and returned a log with a NIL store, so the first Append dereferenced nil -- SIGSEGV, then a second identical one from executeAsync's deferred handler, which appends too, so the recover that exists to record a failure became the crash. Every call site discards the Append error on purpose (best-effort evidence); best-effort must mean 'returns an error', never 'kills the run it was supposed to describe', so the constructor's error is kept and returned by Append/ReadSince. A deliberate, reviewed raise of a shrink-only budget. Rebase integration:  Rebased self-UAT: dev-only MCP registration, committed candidate runner, browser/OAuth isolation, typed cleanup, bounded evidence, and host-proven smoke lifecycle. Deliberate reviewed raise. Ship review: compute temporary browser profiles from the same global state root that creates them, so per-session lease cleanup removes the actual directory. 2878 -> 2902: realMCP.Auth diagnosed a closed browser-capture window two different ways for the same hang. The deadline case and the process-wait case become ready at the same instant -- cmdCtx derives from the same ctx, so an expired window tears the process down and whatever lands on waitErrCh next is that teardown -- and Go picks between ready select cases at random, so the run that passed on the PR failed on the push to main. The deadline now wins once it has passed -- checked on the PARENT ctx as well as captureCtx, since the latter is a child and cancellation reaches a child asynchronously, so the child's Err() can still be nil at the instant the parent releases the wait (a window that widens under -race and failed CI a second time when only the child was checked) -- and both routes share one errBrowserCaptureTimeout() so they cannot drift apart again. A deliberate, reviewed raise of a shrink-only budget. 2910 -> 2934: NewEventLog dropped NewEventStore's error and returned a log with a NIL store, so the first Append dereferenced nil -- SIGSEGV, then a second identical one from executeAsync's deferred handler, which appends too, so the recover that exists to record a failure became the crash. Every call site discards the Append error on purpose (best-effort evidence); best-effort must mean 'returns an error', never 'kills the run it was supposed to describe', so the constructor's error is kept and returned by Append/ReadSince. A deliberate, reviewed raise of a shrink-only budget. 2934 -> 2970, edges 3 -> 4: candidate_smoke now invokes the isolated uatmatrix after building pix and pix-host and before launching the sandbox; the new edge is the intended L3-to-L1 composition, and mock executors get one narrow override so orchestration tests do not pretend to produce runnable candidate binaries. A deliberate, reviewed raise. Then prod_loc 2970 -> 3159: capability discovery reports the validator's legal vocabulary, real build/artifact limits, browser profile state, and candidate_smoke's host-backed memory coverage; submit returns a structured plan sharing the exact resource planner used by execution. Dry runs resolve only paths and never create the fresh browser profile. A deliberate, cross-vendor-reviewed raise of a shrink-only budget. Then U2 of the self-development UAT auth repair (docs/design/self-development-uat.md): capabilitiesResponse gains ExecContext (json exec_context), always SessionWorkerExecContext, so a caller can confirm uat_capabilities was answered by the operator-context pix-host uat-worker and never by a gateway-constructed Runner. prod_loc 3159 -> 3173, exports 60 -> 61 (SessionWorkerExecContext), globals 19 -> 20. A deliberate, reviewed raise of a shrink-only budget. Deliberate reviewed raise: prod_loc 3173 -> 3355, globals 20 -> 21 (the candidate-owned environment-matrix seam, docs/design/self-development-uat.md; host run run-20260823-201941-8f7b648b proved candidate_smoke's env-matrix step still ran the worker's own linked-in uatenvmatrix.RunForCandidateSmoke instead of the SUBMITTED candidate's own code, even after commit 74f74103 fixed fixture materialization). env_matrix.go adds the real envMatrix seam Runner.executeCandidateSmoke now calls: runCandidateEnvMatrix execs `<res.OutDir>/pix-host uat-env-matrix` with fixed, worker-composed argv derived only from RunResources, cwd set to res.OutDir, and no SetEnv call at all -- the child inherits the worker's authenticated environment, exactly the auth channel the prior linked-in call bypassed by never leaving process. It lists the candidate's own --list-checks output as evidence before executing (bounded to envMatrixMaxListedChecks, the one new global), captures stdout/stderr into a bounded steps/env_matrix.log, and reports that artifact on failure. The mock override seam that used to live inside uatenvmatrix as `candidateOverride` moves here instead (NewRunner's exec.(RunCandidateEnvMatrix) check, mirroring memoryMatrix's existing override), so an L1 capability never again needs to know candidate_smoke exists. TestCandidateSmokeNeverCallsLinkedEnvMatrix is the source-level sentinel pinning that this package can never call uatenvmatrix.Run directly again."
    },
    "pix/host/workspace": {
      "prod_loc": 364,
      "exports": 14,
      "globals": 2,
      "edges": 2,
      "exits": 0,
      "_prod_loc_note": "Rebase integration:  Then workspacestate.go's doc comment now names memory-capture alongside the other .pix/* markers it already lists; no code changed, prod_loc 363 -> 364 is a comment-line accounting artifact. A deliberate, reviewed raise of a shrink-only budget."
    }
  }
}
