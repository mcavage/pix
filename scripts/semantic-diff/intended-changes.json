[
  {
    "id": "config-keys.top-level.surface",
    "rationale": "E1.5 (Story 1, native sandbox environments, docs/design/environments.md §5.3) adds two real top-level config.toml keys: `environment` (the machine default environment NAME) and `environments` (the name -> canonical absolute local path registry). Both have no hand-edit path at all — `pix config set/unset` refuses them outright and names the correct `pix env` command (workflow/provision/config.go's environmentKeyRefusal); `pix env use`/`pix env add`/`pix env rm` (Wave C) are the only writers.",
    "evidence": "services/host/config/config.go (Config.Environment / Config.Environments, applyDefaults' dropNoncanonicalEnvironments); services/host/config/environment.go (CanonicalEnvironmentPath/AddEnvironment/RemoveEnvironment/UseEnvironment); services/host/workflow/provision/config.go (environmentKeyRefusal); services/host/config/environment_test.go; services/host/workflow/provision/config_environment_test.go; services/host/cmd/pix/config_environment_cmd_test.go",
    "changes": [
      {
        "file": "services/host/config/config.go",
        "kind": "set",
        "from": ["services", "mcp", "memory_watcher_model", "memory_embed_model", "memory_capture", "ollama_bridge_model", "run_intent", "inference"],
        "to": ["services", "mcp", "memory_watcher_model", "memory_embed_model", "memory_capture", "ollama_bridge_model", "run_intent", "inference", "environment", "environments"]
      }
    ]
  },
  {
    "id": "lifecycle.lease.instance-id-immutable",
    "rationale": "E2.3 review fix (P0-7): lease.CreateRecord's write-once install moved off a direct O_CREAT|O_EXCL open on record.json's own final path, which made write-once a syscall guarantee one syscall too early — a crash between that open and its write left EXACTLY the corrupt/partial file a reader must never see, present and EEXIST-able before a single byte of its content existed. writeRecordOnce now writes the full content to a same-directory 0600 temp file, fsyncs and closes it, and only THEN installs it via os.Link (never os.Rename): link(2), like the O_EXCL open before it, refuses rather than replaces an existing target, so write-once stays exactly the same syscall-level guarantee, just reachable only once the content it publishes is already durable. The identity-match check that used to run only before a create attempt is now the shared checkExistingRecord, reused after a lost create race too: the old EEXIST branch was a bare `return readRecordFile(path)` with no validation, silently handing a racing caller asking for a DIFFERENT instance id the WINNER's record as if it were its own.",
    "evidence": "services/host/lease/record.go (writeRecordOnce, checkExistingRecord, createRecord); services/host/lease/record_test.go (TestCreateRecord_ConcurrentDifferentIdentityLoserIsRefusedNotSilentlyAccepted, TestCreateRecord_ConcurrentSameIdentityAllSucceedIdempotently, TestCreateRecord_NoLeftoverTempFileAfterWrite)",
    "changes": [
      {
        "file": "services/host/lease/record.go",
        "kind": "contains",
        "from": ["if existing.InstanceID != instanceID {", "syscall.O_WRONLY|syscall.O_CREAT|syscall.O_EXCL"],
        "to": ["if existing.InstanceID != instanceID {", "os.Link(tmpPath, path)", "errors.Is(werr, os.ErrExist)"]
      }
    ]
  }
]
