[
	{
		"id": "lifecycle.session.record-before-lifecycle-unlock",
		"rationale": "E2.5 escalation (BLOCK 3): RunSession's lease-failure fallback is DELETED. It used to warn and then spawn spec.CreateArgs with no lease at all \u2014 after the cutover that is a second, selectable create path (the exact thing PRD \u00a78 forbids), and it created a sandbox nothing on this host could prove it owned. A lease/state-dir failure is now a hard SessionRefused before anything spawns, so the literal `return child.Wait()` that belonged to that fallback is gone; the invariant this pin exists for is untouched and still pinned by the two surviving literals (the transition runs inside AttachRefUnderLifecycle's fn, and the child is waited for only after the refs reference is held).",
		"evidence": "services/host/workflow/launch/session.go (RunSession's lease-dir refusal); services/host/workflow/launch/envcutover_test.go (TestRunSession_LeaseDirFailure_RefusesAndSpawnsNothing, TestRunSession_CreateWithoutEffectiveEnvironmentRefuses, TestRunSession_CreateSpawnsEnvCreateThenExec_NeverSbxRun)",
		"changes": [
			{
				"file": "services/host/workflow/launch/session.go",
				"kind": "contains",
				"from": [
					"ref, err := lease.AttachRefUnderLifecycle(ctx, dir, func() error {",
					"child, terr = startSessionTransition(spec, deps)",
					"return child.Wait()"
				],
				"to": [
					"ref, err := lease.AttachRefUnderLifecycle(ctx, dir, func() error {",
					"child, terr = startSessionTransition(spec, deps)",
					"werr := child.Wait()"
				]
			}
		]
	},
	{
		"id": "lifecycle.session.create-record-requires-verified-instance-id",
		"rationale": "E2.5 escalation (BLOCK 6): the create no longer merely RECORDS a lease, it PROMOTES the bounded create intent into the instance-bound, write-once receipt (E2.3's PromoteCreateIntent), which binds instance id AND name and clears the intent. Recording therefore happens only on the create path's own promotion step, so startSessionTransition's attach branch no longer computes `recorded := SessionRecorded(spec.Key)` for a create it does not perform \u2014 it consults SessionRecorded only to decide whether -k/--keep has a record to bind to. The verified-instance-id requirement this pin exists for is unchanged and still pinned by the two surviving literals, and is now additionally enforced in PromoteSessionCreation.",
		"evidence": "services/host/workflow/launch/session.go (PromoteSessionCreation, startEnvCreateTransition, startSessionTransition's keep branch); services/host/workflow/launch/envcutover_test.go (TestRunSession_CreatePromotesIntentToAReceipt, TestRunSession_NoReceipt_RefusesWithZeroRemovals)",
		"changes": [
			{
				"file": "services/host/workflow/launch/session.go",
				"kind": "contains",
				"from": [
					"if found == nil || !found.IdentityVerified || found.InstanceID == nil || *found.InstanceID == \"\" {",
					"return false, nil // present but unowned: no verifiable instance id to record",
					"recorded := SessionRecorded(spec.Key)"
				],
				"to": [
					"if found == nil || !found.IdentityVerified || found.InstanceID == nil || *found.InstanceID == \"\" {",
					"return false, nil // present but unowned: no verifiable instance id to record",
					"case !SessionRecorded(spec.Key):"
				]
			}
		]
	},
	{
		"id": "argv.op-run-wrapper.exact-grammar",
		"rationale": "Resolve only each MCP server\u2019s declared references at spawn time. The wrapper retains op run, uses a filtered in-memory env file, and execs op to preserve signals; unrelated provider references cannot block or leak into integrations.",
		"evidence": "services/host/workflow/env/mcp_test.go: TestEnvironmentFacts_CredentialsAreScopedAndReadAtSpawn; setup Google callback regression in the environment repository",
		"changes": [
			{
				"file": "services/host/mcp/mcp.go",
				"kind": "contains",
				"from": [
					"return append([]string{opPath, \"run\", \"--no-masking\", \"--env-file=\" + opRefs, \"--\"}, argv...)"
				],
				"to": [
					"return append([]string{\"/bin/bash\", \"-c\", scopedOpRun, \"pix-mcp\", opPath, opRefs, strings.Join(keys, \",\")}, argv...)"
				]
			}
		]
	}
]
